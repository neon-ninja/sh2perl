Perl-Specific Code Search Results
=================================

Total matches found: 263

Perl Functions (263 matches)
--------------------------------------------------
File: generator\control_flow.rs:100
Pattern: word_to_perl
Context:
      97:                 perl_pattern = perl_pattern.replace("]", "\\]");
      98: 
      99:                 // Create condition: $operation =~ /^pattern$/
>>>  100:                 let word_str = generator.word_to_perl(&case_stmt.word);
     101: 
     102:                 // Handle positional parameters in case statements
     103:                 let processed_word = if word_str.contains("$1") || word_str.contains("$2") || word_str.contains("$3") {

File: generator\control_flow.rs:107
Pattern: word_to_perl
Context:
     104:                     // Replace positional parameters with generic names that will be replaced later
     105:                     word_str.replace("$1", "$arg1").replace("$2", "$arg2").replace("$3", "$arg3")
     106:                 } else if word_str.contains("$name") {
>>>  107:                     // The word_to_perl converted $1 to $name, but we need $arg1 for parameter replacement
     108:                     word_str.replace("$name", "$arg1")
     109:                 } else {
     110:                     word_str

File: generator\control_flow.rs:233
Pattern: word_to_perl
Context:
     230:                         match var.as_str() {
     231:                             "@" => all_items.push("@ARGV".to_string()),  // $@ -> @ARGV (no quotes)
     232:                             "*" => all_items.push("@ARGV".to_string()),  // $* -> @ARGV (no quotes)
>>>  233:                             _ => all_items.push(generator.word_to_perl(word))
     234:                         }
     235:                     } else if let StringPart::ParameterExpansion(pe) = &interp.parts[0] {
     236:                         // Handle ${arr[@]} -> @arr for array iteration or ${!map[@]} -> keys %map for map keys

File: generator\control_flow.rs:247
Pattern: word_to_perl
Context:
     244:                                 all_items.push(format!("@{}", pe.variable));
     245:                             }
     246:                         } else {
>>>  247:                             all_items.push(generator.word_to_perl(word));
     248:                         }
     249:                     } else {
     250:                         all_items.push(generator.word_to_perl(word));

File: generator\control_flow.rs:250
Pattern: word_to_perl
Context:
     247:                             all_items.push(generator.word_to_perl(word));
     248:                         }
     249:                     } else {
>>>  250:                         all_items.push(generator.word_to_perl(word));
     251:                     }
     252:                 } else {
     253:                     all_items.push(generator.word_to_perl(word));

File: generator\control_flow.rs:253
Pattern: word_to_perl
Context:
     250:                         all_items.push(generator.word_to_perl(word));
     251:                     }
     252:                 } else {
>>>  253:                     all_items.push(generator.word_to_perl(word));
     254:                 }
     255:             }
     256:             Word::BraceExpansion(expansion) => {

File: generator\control_flow.rs:286
Pattern: word_to_perl
Context:
     283:                                 }
     284:                             } else {
     285:                                 // Fallback for non-numeric ranges
>>>  286:                                 all_items.push(generator.word_to_perl(word));
     287:                             }
     288:                         }
     289:                         BraceItem::Literal(s) => {

File: generator\control_flow.rs:348
Pattern: word_to_perl
Context:
     345:                         .collect();
     346:                     all_items.extend(items);
     347:                 } else {
>>>  348:                     all_items.push(generator.word_to_perl(word));
     349:                 }
     350:             }
     351:             _ => all_items.push(generator.word_to_perl(word))

File: generator\control_flow.rs:351
Pattern: word_to_perl
Context:
     348:                     all_items.push(generator.word_to_perl(word));
     349:                 }
     350:             }
>>>  351:             _ => all_items.push(generator.word_to_perl(word))
     352:         }
     353:     }
     354: 

File: generator\mod.rs:158
Pattern: word_to_perl
Context:
     155:         redirects::generate_redirect_impl(self, redirect)
     156:     }
     157: 
>>>  158:     pub fn word_to_perl(&mut self, word: &Word) -> String {
     159:         words::word_to_perl_impl(self, word)
     160:     }
     161: 

File: generator\mod.rs:159
Pattern: word_to_perl
Context:
     156:     }
     157: 
     158:     pub fn word_to_perl(&mut self, word: &Word) -> String {
>>>  159:         words::word_to_perl_impl(self, word)
     160:     }
     161: 
     162:     pub fn word_to_perl_for_test(&mut self, word: &Word) -> String {

File: generator\mod.rs:162
Pattern: word_to_perl
Context:
     159:         words::word_to_perl_impl(self, word)
     160:     }
     161: 
>>>  162:     pub fn word_to_perl_for_test(&mut self, word: &Word) -> String {
     163:         words::word_to_perl_for_test_impl(self, word)
     164:     }
     165: 

File: generator\mod.rs:163
Pattern: word_to_perl
Context:
     160:     }
     161: 
     162:     pub fn word_to_perl_for_test(&mut self, word: &Word) -> String {
>>>  163:         words::word_to_perl_for_test_impl(self, word)
     164:     }
     165: 
     166:     pub fn generate_parameter_expansion(&mut self, pe: &ParameterExpansion) -> String {

File: generator\utils.rs:79
Pattern: word_to_perl
Context:
      76:             // Handle command substitution
      77:             match cmd.as_ref() {
      78:                 Command::Simple(simple_cmd) => {
>>>   79:                     let cmd_name = generator.word_to_perl(&simple_cmd.name);
      80:                     let args: Vec<String> = simple_cmd.args.iter()
      81:                         .map(|arg| generator.word_to_perl(arg))
      82:                         .collect();

File: generator\utils.rs:81
Pattern: word_to_perl
Context:
      78:                 Command::Simple(simple_cmd) => {
      79:                     let cmd_name = generator.word_to_perl(&simple_cmd.name);
      80:                     let args: Vec<String> = simple_cmd.args.iter()
>>>   81:                         .map(|arg| generator.word_to_perl(arg))
      82:                         .collect();
      83: 
      84:                     // For simple commands, fall back to system command for now

File: generator\words.rs:5
Pattern: word_to_perl
Context:
       2: use super::Generator;
       3: use regex::Regex;
       4: 
>>>    5: pub fn word_to_perl_impl(generator: &mut Generator, word: &Word) -> String {
       6:     match word {
       7:         Word::Literal(s) => {
       8:             // Handle literal strings

File: generator\words.rs:76
Pattern: word_to_perl
Context:
      73:     }
      74: }
      75: 
>>>   76: pub fn word_to_perl_for_test_impl(generator: &mut Generator, word: &Word) -> String {
      77:     match word {
      78:         Word::Literal(s) => s.clone(),
      79:         Word::ParameterExpansion(pe) => generator.generate_parameter_expansion(pe),

File: generator\words.rs:117
Pattern: word_to_perl
Context:
     114:     let suffix = expansion.suffix.as_deref().unwrap_or("");
     115: 
     116:     if expansion.items.len() == 1 {
>>>  117:         let expanded = generator.word_to_perl(&generator.brace_item_to_word(&expansion.items[0]));
     118:         if !prefix.is_empty() || !suffix.is_empty() {
     119:             // Split the expanded items and add prefix/suffix to each
     120:             let items: Vec<String> = expanded.split_whitespace()

File: generator\words.rs:134
Pattern: word_to_perl
Context:
     131:                 let word = generator.brace_item_to_word(item);
     132:                 match word {
     133:                     Word::Literal(s) => vec![s],
>>>  134:                     _ => vec![generator.word_to_perl(&word)],
     135:                 }
     136:             })
     137:             .collect();

File: generator\commands\basename.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // basename command syntax: basename path [suffix]
       8:     if let Some(path) = cmd.args.first() {
>>>    9:         let path_str = generator.word_to_perl(path);
      10:         let suffix = if cmd.args.len() > 1 {
      11:             generator.word_to_perl(&cmd.args[1])
      12:         } else {

File: generator\commands\basename.rs:11
Pattern: word_to_perl
Context:
       8:     if let Some(path) = cmd.args.first() {
       9:         let path_str = generator.word_to_perl(path);
      10:         let suffix = if cmd.args.len() > 1 {
>>>   11:             generator.word_to_perl(&cmd.args[1])
      12:         } else {
      13:             "".to_string()
      14:         };

File: generator\commands\cat.rs:27
Pattern: word_to_perl
Context:
      24:             // Reconstruct the filename from split arguments if needed
      25:             if cmd.args.len() > 1 {
      26:                 cmd.args.iter()
>>>   27:                     .map(|arg| generator.word_to_perl(arg))
      28:                     .collect::<Vec<_>>()
      29:                     .join("")
      30:             } else {

File: generator\commands\cat.rs:31
Pattern: word_to_perl
Context:
      28:                     .collect::<Vec<_>>()
      29:                     .join("")
      30:             } else {
>>>   31:                 generator.word_to_perl(&cmd.args[0])
      32:             }
      33:         };
      34: 

File: generator\commands\command_dispatcher.rs:514
Pattern: word_to_perl
Context:
     511:                 });
     512: 
     513:                 if let Some(redirect) = output_redirect {
>>>  514:                     let target = generator.word_to_perl(&redirect.target);
     515:                     let mode = if matches!(redirect.operator, RedirectOperator::Append) { ">>" } else { ">" };
     516:                     result.push_str(&generator.indent());
     517:                     result.push_str(&format!("open(STDOUT, '{}', '{}') or die \"Cannot open file: $!\";\n", mode, target));

File: generator\commands\cp.rs:20
Pattern: word_to_perl
Context:
      17:                 "-p" | "--preserve" => preserve = true,
      18:                 _ => {
      19:                     if !arg_str.starts_with('-') {
>>>   20:                         sources.push(generator.word_to_perl(arg));
      21:                     }
      22:                 }
      23:             }

File: generator\commands\cp.rs:25
Pattern: word_to_perl
Context:
      22:                 }
      23:             }
      24:         } else {
>>>   25:             sources.push(generator.word_to_perl(arg));
      26:         }
      27:     }
      28: 

File: generator\commands\curl.rs:26
Pattern: word_to_perl
Context:
      23:                 "-X" | "--request" => {
      24:                     if i + 1 < cmd.args.len() {
      25:                         if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   26:                             method = generator.word_to_perl(next_arg);
      27:                             i += 1;
      28:                         }
      29:                     }

File: generator\commands\curl.rs:34
Pattern: word_to_perl
Context:
      31:                 "-H" | "--header" => {
      32:                     if i + 1 < cmd.args.len() {
      33:                         if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   34:                             headers.push(generator.word_to_perl(next_arg));
      35:                             i += 1;
      36:                         }
      37:                     }

File: generator\commands\curl.rs:42
Pattern: word_to_perl
Context:
      39:                 "-d" | "--data" => {
      40:                     if i + 1 < cmd.args.len() {
      41:                         if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   42:                             data = generator.word_to_perl(next_arg);
      43:                             i += 1;
      44:                         }
      45:                     }

File: generator\commands\curl.rs:50
Pattern: word_to_perl
Context:
      47:                 "-o" | "--output" => {
      48:                     if i + 1 < cmd.args.len() {
      49:                         if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   50:                             output_file = generator.word_to_perl(next_arg);
      51:                             i += 1;
      52:                         }
      53:                     }

File: generator\commands\curl.rs:72
Pattern: word_to_perl
Context:
      69:                 }
      70:                 _ => {
      71:                     if !arg_str.starts_with('-') && url.is_empty() {
>>>   72:                         url = generator.word_to_perl(&cmd.args[i]);
      73:                     }
      74:                 }
      75:             }

File: generator\commands\curl.rs:78
Pattern: word_to_perl
Context:
      75:             }
      76:         } else {
      77:             if url.is_empty() {
>>>   78:                 url = generator.word_to_perl(&cmd.args[i]);
      79:             }
      80:         }
      81:         i += 1;

File: generator\commands\cut.rs:17
Pattern: word_to_perl
Context:
      14:         if let Word::Literal(arg) = &cmd.args[i] {
      15:             if arg == "-d" && i + 1 < cmd.args.len() {
      16:                 if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   17:                     delimiter = generator.word_to_perl(next_arg);
      18:                     i += 1; // Skip the delimiter argument
      19:                 }
      20:             } else if arg == "-f" && i + 1 < cmd.args.len() {

File: generator\commands\cut.rs:22
Pattern: word_to_perl
Context:
      19:                 }
      20:             } else if arg == "-f" && i + 1 < cmd.args.len() {
      21:                 if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   22:                     _fields = generator.word_to_perl(next_arg);
      23:                     i += 1; // Skip the fields argument
      24:                 }
      25:             }

File: generator\commands\date.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // date command syntax: date [format]
       8:     if let Some(format) = cmd.args.first() {
>>>    9:         let format_str = generator.word_to_perl(format);
      10: 
      11:         output.push_str("use POSIX qw(strftime);\n");
      12:         output.push_str(&format!("my $format = {};\n", format_str));

File: generator\commands\dirname.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // dirname command syntax: dirname path
       8:     if let Some(path) = cmd.args.first() {
>>>    9:         let path_str = generator.word_to_perl(path);
      10: 
      11:         output.push_str(&format!("my $path = {};\n", path_str));
      12:         output.push_str("if ($path =~ /\\//) {\n");

File: generator\commands\find.rs:52
Pattern: word_to_perl
Context:
      49:                                 .collect::<Vec<_>>()
      50:                                 .join("")
      51:                         },
>>>   52:                         _ => generator.word_to_perl(next_arg)
      53:                     };
      54:                     i += 1; // Skip the pattern argument
      55:                 }

File: generator\commands\find.rs:59
Pattern: word_to_perl
Context:
      56:             } else if arg == "-mtime" && i + 1 < cmd.args.len() {
      57:                 find_type = "mtime";
      58:                 if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   59:                     find_value = generator.word_to_perl(next_arg);
      60:                     i += 1;
      61:                 }
      62:             } else if arg == "-mmin" && i + 1 < cmd.args.len() {

File: generator\commands\find.rs:65
Pattern: word_to_perl
Context:
      62:             } else if arg == "-mmin" && i + 1 < cmd.args.len() {
      63:                 find_type = "mmin";
      64:                 if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   65:                     find_value = generator.word_to_perl(next_arg);
      66:                     i += 1;
      67:                 }
      68:             } else if arg == "-size" && i + 1 < cmd.args.len() {

File: generator\commands\find.rs:71
Pattern: word_to_perl
Context:
      68:             } else if arg == "-size" && i + 1 < cmd.args.len() {
      69:                 find_type = "size";
      70:                 if let Some(next_arg) = cmd.args.get(i + 1) {
>>>   71:                     find_value = generator.word_to_perl(next_arg);
      72:                     i += 1;
      73:                 }
      74:             } else if arg == "-empty" {

File: generator\commands\find.rs:85
Pattern: word_to_perl
Context:
      82:                         if arg == ";" {
      83:                             break;
      84:                         }
>>>   85:                         exec_args.push(generator.word_to_perl(&cmd.args[i]));
      86:                     }
      87:                     i += 1;
      88:                 }

File: generator\commands\find.rs:96
Pattern: word_to_perl
Context:
      93:                 if let Word::Literal(next_arg) = &cmd.args[i + 1] {
      94:                     if next_arg == "-path" {
      95:                         if let Some(path_arg) = cmd.args.get(i + 2) {
>>>   96:                             exclude_paths.push(generator.word_to_perl(path_arg));
      97:                             i += 2; // Skip -path and the path argument
      98:                         }
      99:                     }

File: generator\commands\grep.rs:126
Pattern: word_to_perl
Context:
     123:             }
     124:         } else if pattern.is_empty() {
     125:             // First non-literal argument is the pattern
>>>  126:             pattern = generator.word_to_perl(arg);
     127:         }
     128:     }
     129: 

File: generator\commands\gzip.rs:26
Pattern: word_to_perl
Context:
      23:                 "-v" | "--verbose" => {}, // Verbose output (handled by gzip)
      24:                 _ => {
      25:                     if !arg_str.starts_with('-') {
>>>   26:                         files.push(generator.word_to_perl(arg));
      27:                     }
      28:                 }
      29:             }

File: generator\commands\gzip.rs:31
Pattern: word_to_perl
Context:
      28:                 }
      29:             }
      30:         } else {
>>>   31:             files.push(generator.word_to_perl(arg));
      32:         }
      33:     }
      34: 

File: generator\commands\kill.rs:26
Pattern: word_to_perl
Context:
      23:                 }
      24:             } else {
      25:                 // Non-option argument is a PID
>>>   26:                 pids.push(generator.word_to_perl(arg));
      27:             }
      28:         } else {
      29:             pids.push(generator.word_to_perl(arg));

File: generator\commands\kill.rs:29
Pattern: word_to_perl
Context:
      26:                 pids.push(generator.word_to_perl(arg));
      27:             }
      28:         } else {
>>>   29:             pids.push(generator.word_to_perl(arg));
      30:         }
      31:     }
      32: 

File: generator\commands\mkdir.rs:18
Pattern: word_to_perl
Context:
      15:                 "-p" | "--parents" => create_parents = true,
      16:                 _ => {
      17:                     if !arg_str.starts_with('-') {
>>>   18:                         directories.push(generator.word_to_perl(arg));
      19:                     }
      20:                 }
      21:             }

File: generator\commands\mkdir.rs:23
Pattern: word_to_perl
Context:
      20:                 }
      21:             }
      22:         } else {
>>>   23:             directories.push(generator.word_to_perl(arg));
      24:         }
      25:     }
      26: 

File: generator\commands\mv.rs:18
Pattern: word_to_perl
Context:
      15:                 "-f" | "--force" => _force = true,
      16:                 _ => {
      17:                     if !arg_str.starts_with('-') {
>>>   18:                         sources.push(generator.word_to_perl(arg));
      19:                     }
      20:                 }
      21:             }

File: generator\commands\mv.rs:23
Pattern: word_to_perl
Context:
      20:                 }
      21:             }
      22:         } else {
>>>   23:             sources.push(generator.word_to_perl(arg));
      24:         }
      25:     }
      26: 

File: generator\commands\nice.rs:45
Pattern: word_to_perl
Context:
      42:     if command_args.is_empty() {
      43:         output.push_str("die \"nice: missing command\\n\";\n");
      44:     } else {
>>>   45:         let command = generator.word_to_perl(&command_args[0]);
      46:         let args: Vec<String> = command_args[1..].iter()
      47:             .map(|arg| generator.word_to_perl(arg))
      48:             .collect();

File: generator\commands\nice.rs:47
Pattern: word_to_perl
Context:
      44:     } else {
      45:         let command = generator.word_to_perl(&command_args[0]);
      46:         let args: Vec<String> = command_args[1..].iter()
>>>   47:             .map(|arg| generator.word_to_perl(arg))
      48:             .collect();
      49: 
      50:         output.push_str("use POSIX qw(setpriority PRIO_PROCESS);\n");

File: generator\commands\nohup.rs:12
Pattern: word_to_perl
Context:
       9:         output.push_str("die \"nohup: missing command\\n\";\n");
      10:     } else {
      11:         // Get the command to run
>>>   12:         let command = generator.word_to_perl(&cmd.args[0]);
      13:         let args: Vec<String> = cmd.args[1..].iter()
      14:             .map(|arg| generator.word_to_perl(arg))
      15:             .collect();

File: generator\commands\nohup.rs:14
Pattern: word_to_perl
Context:
      11:         // Get the command to run
      12:         let command = generator.word_to_perl(&cmd.args[0]);
      13:         let args: Vec<String> = cmd.args[1..].iter()
>>>   14:             .map(|arg| generator.word_to_perl(arg))
      15:             .collect();
      16: 
      17:         output.push_str("use POSIX qw(setsid);\n");

File: generator\commands\paste.rs:73
Pattern: word_to_perl
Context:
      70:     } else {
      71:         // Handle regular paste command (fallback to system)
      72:         let args: Vec<String> = cmd.args.iter()
>>>   73:             .map(|arg| generator.word_to_perl(arg))
      74:             .collect();
      75: 
      76:         if !args.is_empty() {

File: generator\commands\pipeline_commands.rs:53
Pattern: word_to_perl
Context:
      50:                                     match var.as_str() {
      51:                                         "@" => all_items.push("@ARGV".to_string()),
      52:                                         "*" => all_items.push("@ARGV".to_string()),
>>>   53:                                         _ => all_items.push(generator.word_to_perl(word))
      54:                                     }
      55:                                 } else if let StringPart::ParameterExpansion(pe) = &interp.parts[0] {
      56:                                     if pe.operator == ParameterExpansionOperator::ArraySlice("@".to_string(), None) {

File: generator\commands\pipeline_commands.rs:64
Pattern: word_to_perl
Context:
      61:                                             all_items.push(format!("@{}", pe.variable));
      62:                                         }
      63:                                     } else {
>>>   64:                                         all_items.push(generator.word_to_perl(word));
      65:                                     }
      66:                                 } else {
      67:                                     all_items.push(generator.word_to_perl(word));

File: generator\commands\pipeline_commands.rs:67
Pattern: word_to_perl
Context:
      64:                                         all_items.push(generator.word_to_perl(word));
      65:                                     }
      66:                                 } else {
>>>   67:                                     all_items.push(generator.word_to_perl(word));
      68:                                 }
      69:                             } else {
      70:                                 all_items.push(generator.word_to_perl(word));

File: generator\commands\pipeline_commands.rs:70
Pattern: word_to_perl
Context:
      67:                                     all_items.push(generator.word_to_perl(word));
      68:                                 }
      69:                             } else {
>>>   70:                                 all_items.push(generator.word_to_perl(word));
      71:                             }
      72:                         }
      73:                         _ => all_items.push(generator.word_to_perl(word))

File: generator\commands\pipeline_commands.rs:73
Pattern: word_to_perl
Context:
      70:                                 all_items.push(generator.word_to_perl(word));
      71:                             }
      72:                         }
>>>   73:                         _ => all_items.push(generator.word_to_perl(word))
      74:                     }
      75:                 }
      76:                 output.push_str(&all_items.join(", "));

File: generator\commands\pipeline_commands.rs:585
Pattern: word_to_perl
Context:
     582:             if let Some(sed_expr) = cmd.args.iter().find(|arg| {
     583:                 if let Word::Literal(s) = arg { s.starts_with('s') } else { false }
     584:             }) {
>>>  585:                 let expr = generator.word_to_perl(sed_expr);
     586:                 output.push_str(&format!("$line =~ {expr};\n"));
     587:             }
     588:             output

File: generator\commands\printf.rs:14
Pattern: word_to_perl
Context:
      11:     for (i, arg) in cmd.args.iter().enumerate() {
      12:         if i == 0 {
      13:             // First argument is the format string
>>>   14:             format_string = generator.word_to_perl(arg);
      15:             // Remove quotes if they exist around the format string
      16:             if format_string.starts_with('\'') && format_string.ends_with('\'') {
      17:                 format_string = format_string[1..format_string.len()-1].to_string();

File: generator\commands\printf.rs:23
Pattern: word_to_perl
Context:
      20:             }
      21:         } else {
      22:             // Subsequent arguments are the values to format
>>>   23:             args.push(generator.word_to_perl(arg));
      24:         }
      25:     }
      26: 

File: generator\commands\rm.rs:25
Pattern: word_to_perl
Context:
      22:                 }
      23:             }
      24:         } else {
>>>   25:             files.push(generator.word_to_perl(arg));
      26:         }
      27:     }
      28: 

File: generator\commands\sha256sum.rs:17
Pattern: word_to_perl
Context:
      14:             if arg_str == "-c" {
      15:                 check_mode = true;
      16:             } else if !arg_str.starts_with('-') {
>>>   17:                 files.push(generator.word_to_perl(arg));
      18:             }
      19:         } else {
      20:             files.push(generator.word_to_perl(arg));

File: generator\commands\sha256sum.rs:20
Pattern: word_to_perl
Context:
      17:                 files.push(generator.word_to_perl(arg));
      18:             }
      19:         } else {
>>>   20:             files.push(generator.word_to_perl(arg));
      21:         }
      22:     }
      23: 

File: generator\commands\sha512sum.rs:17
Pattern: word_to_perl
Context:
      14:             if arg_str == "-c" {
      15:                 check_mode = true;
      16:             } else if !arg_str.starts_with('-') {
>>>   17:                 files.push(generator.word_to_perl(arg));
      18:             }
      19:         } else {
      20:             files.push(generator.word_to_perl(arg));

File: generator\commands\sha512sum.rs:20
Pattern: word_to_perl
Context:
      17:                 files.push(generator.word_to_perl(arg));
      18:             }
      19:         } else {
>>>   20:             files.push(generator.word_to_perl(arg));
      21:         }
      22:     }
      23: 

File: generator\commands\simple_commands.rs:353
Pattern: word_to_perl
Context:
     350:                                     // Handle brace expansion for command arguments
     351:                                     handle_brace_expansion_for_command(generator, expansion)
     352:                                 }
>>>  353:                                 _ => generator.word_to_perl(arg)
     354:                             }
     355:                         })
     356:                         .collect();

File: generator\commands\simple_commands.rs:374
Pattern: word_to_perl
Context:
     371:                                     // Handle brace expansion for command arguments
     372:                                     handle_brace_expansion_for_command(generator, expansion)
     373:                                 }
>>>  374:                                 _ => generator.word_to_perl(arg)
     375:                             }
     376:                         })
     377:                         .collect();

File: generator\commands\simple_commands.rs:394
Pattern: word_to_perl
Context:
     391:             output.push_str(&format!("system('{}');\n", cmd_name));
     392:         } else {
     393:             let args: Vec<String> = cmd.args.iter()
>>>  394:                 .map(|arg| generator.word_to_perl(arg))
     395:                 .collect();
     396:             output.push_str(&generator.indent());
     397:             output.push_str(&format!("system('{}', {});\n", cmd_name, args.join(", ")));

File: generator\commands\simple_commands.rs:701
Pattern: word_to_perl
Context:
     698:             }
     699:             _ => {
     700:                 // Convert non-brace arguments to Perl strings
>>>  701:                 non_brace_args.push(generator.word_to_perl(arg));
     702:             }
     703:         }
     704:     }

File: generator\commands\simple_commands.rs:709
Pattern: word_to_perl
Context:
     706:     if expansions.is_empty() {
     707:         // No brace expansions, fall back to simple joining
     708:         let args_str = args.iter()
>>>  709:             .map(|arg| generator.word_to_perl(arg))
     710:             .collect::<Vec<_>>()
     711:             .join(" . \" \" . ");
     712:         output.push_str(&generator.indent());

File: generator\commands\sleep.rs:10
Pattern: word_to_perl
Context:
       7:     // sleep command syntax: sleep [number][suffix]
       8:     // Supports seconds (default), minutes (m), hours (h), days (d)
       9:     if let Some(duration) = cmd.args.first() {
>>>   10:         let duration_str = generator.word_to_perl(duration);
      11: 
      12:         output.push_str("use Time::HiRes qw(sleep);\n");
      13:         output.push_str(&format!("sleep({});\n", duration_str));

File: generator\commands\system_commands.rs:8
Pattern: word_to_perl
Context:
       5:     match cmd {
       6:         Command::Simple(simple_cmd) => {
       7:             let args: Vec<String> = simple_cmd.args.iter()
>>>    8:                 .map(|arg| generator.word_to_perl(arg))
       9:                 .collect();
      10:             if args.is_empty() {
      11:                 simple_cmd.name.to_string()

File: generator\commands\system_commands.rs:21
Pattern: word_to_perl
Context:
      18:                 .filter_map(|cmd| {
      19:                     if let Command::Simple(simple_cmd) = cmd {
      20:                         let args: Vec<String> = simple_cmd.args.iter()
>>>   21:                             .map(|arg| generator.word_to_perl(arg))
      22:                             .collect();
      23:                         if args.is_empty() {
      24:                             Some(simple_cmd.name.to_string())

File: generator\commands\system_commands.rs:39
Pattern: word_to_perl
Context:
      36:             match &**subshell_cmd {
      37:                 Command::Simple(simple_cmd) => {
      38:                     let args: Vec<String> = simple_cmd.args.iter()
>>>   39:                         .map(|arg| generator.word_to_perl(arg))
      40:                         .collect();
      41:                     if args.is_empty() {
      42:                         simple_cmd.name.to_string()

File: generator\commands\system_commands.rs:52
Pattern: word_to_perl
Context:
      49:                         .filter_map(|cmd| {
      50:                             if let Command::Simple(simple_cmd) = cmd {
      51:                                 let args: Vec<String> = simple_cmd.args.iter()
>>>   52:                                     .map(|arg| generator.word_to_perl(arg))
      53:                                     .collect();
      54:                                 Some(format!("{} {}", simple_cmd.name, args.join(" ")))
      55:                             } else {

File: generator\commands\tee.rs:17
Pattern: word_to_perl
Context:
      14:             if arg_str == "-a" {
      15:                 append_mode = true;
      16:             } else if !arg_str.starts_with('-') {
>>>   17:                 files.push(generator.word_to_perl(arg));
      18:             }
      19:         } else {
      20:             files.push(generator.word_to_perl(arg));

File: generator\commands\tee.rs:20
Pattern: word_to_perl
Context:
      17:                 files.push(generator.word_to_perl(arg));
      18:             }
      19:         } else {
>>>   20:             files.push(generator.word_to_perl(arg));
      21:         }
      22:     }
      23: 

File: generator\commands\time.rs:14
Pattern: word_to_perl
Context:
      11: 
      12:     // Execute the command (if any arguments provided)
      13:     if let Some(command) = cmd.args.first() {
>>>   14:         let command_str = generator.word_to_perl(command);
      15:         output.push_str(&format!("system({});\n", command_str));
      16:     }
      17: 

File: generator\commands\touch.rs:52
Pattern: word_to_perl
Context:
      49:                                 }
      50:                             } else {
      51:                                 // Fallback for non-numeric ranges
>>>   52:                                 files.push(generator.word_to_perl(arg));
      53:                                 continue;
      54:                             }
      55:                         }

File: generator\commands\touch.rs:107
Pattern: word_to_perl
Context:
     104:                 files.push(format!("BRACE_EXPANSION:{}", expanded_items.join(" ")));
     105:             }
     106:             _ => {
>>>  107:                 files.push(generator.word_to_perl(arg));
     108:             }
     109:         }
     110:     }

File: generator\commands\wget.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // wget command syntax: wget [options] URL
       8:     if let Some(url) = cmd.args.last() {
>>>    9:         let url_str = generator.word_to_perl(url);
      10:         let mut output_file = "".to_string();
      11: 
      12:         // Parse wget options

File: generator\commands\wget.rs:20
Pattern: word_to_perl
Context:
      17:                     if let Some(_next_arg) = cmd.args.iter().find(|&a| a == arg) {
      18:                         if let Some(idx) = cmd.args.iter().position(|a| a == arg) {
      19:                             if idx + 1 < cmd.args.len() {
>>>   20:                                 output_file = generator.word_to_perl(&cmd.args[idx + 1]);
      21:                             }
      22:                         }
      23:                     }

File: generator\commands\which.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // which command syntax: which command
       8:     if let Some(command) = cmd.args.first() {
>>>    9:         let command_str = generator.word_to_perl(command);
      10: 
      11:         output.push_str(&format!("my $command = {};\n", command_str));
      12:         output.push_str("my $found = 0;\n");

File: generator\commands\yes.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // yes command syntax: yes [string]
       8:     let string_to_repeat = if let Some(arg) = cmd.args.first() {
>>>    9:         generator.word_to_perl(arg)
      10:     } else {
      11:         "y".to_string()
      12:     };

File: generator\commands\zcat.rs:9
Pattern: word_to_perl
Context:
       6: 
       7:     // zcat command syntax: zcat file
       8:     if let Some(filename) = cmd.args.first() {
>>>    9:         let filename_str = generator.word_to_perl(filename);
      10: 
      11:         output.push_str(&format!("my $filename = {};\n", filename_str));
      12:         output.push_str("if (!-f $filename) {\n");

File: generator\control_flow.rs:87
Pattern: perl_string_literal
Context:
      84:         // Handle multiple patterns in a single case clause
      85:         let mut pattern_conditions = Vec::new();
      86:         for pattern in &case_clause.patterns {
>>>   87:             let pattern_str = generator.perl_string_literal(pattern);
      88:             if pattern_str == "\"*\"" {
      89:                 // Default case - this should be the last one
      90:                 pattern_conditions.push("1".to_string()); // Always true

File: generator\control_flow.rs:458
Pattern: perl_string_literal
Context:
     455: pub fn generate_return_statement_impl(generator: &mut Generator, value: &Option<Word>) -> String {
     456:     match value {
     457:         Some(word) => {
>>>  458:             let perl_value = generator.perl_string_literal(word);
     459:             format!("return {};", perl_value)
     460:         }
     461:         None => "return;".to_string(),

File: generator\mod.rs:178
Pattern: perl_string_literal
Context:
     175:         utils::extract_array_elements_impl(value)
     176:     }
     177: 
>>>  178:     pub fn perl_string_literal(&mut self, word: &Word) -> String {
     179:         utils::perl_string_literal_impl(self, word)
     180:     }
     181: 

File: generator\mod.rs:179
Pattern: perl_string_literal
Context:
     176:     }
     177: 
     178:     pub fn perl_string_literal(&mut self, word: &Word) -> String {
>>>  179:         utils::perl_string_literal_impl(self, word)
     180:     }
     181: 
     182:     pub fn strip_shell_quotes_and_convert_to_perl(&mut self, word: &Word) -> String {

File: generator\redirects.rs:30
Pattern: perl_string_literal
Context:
      27:             // Heredoc: command << delimiter
      28:             if let Some(body) = &redirect.heredoc_body {
      29:                 // Create a temporary file with the heredoc content
>>>   30:                 output.push_str(&format!("my $temp_content = {};\n", generator.perl_string_literal(&Word::Literal(body.clone()))));
      31:                 let fh = generator.get_unique_file_handle();
      32:                 output.push_str(&format!("use File::Path qw(make_path);\n"));
      33:                 output.push_str(&format!("make_path('{}') unless -d '{}';\n", get_temp_dir(), get_temp_dir()));

File: generator\redirects.rs:266
Pattern: perl_string_literal
Context:
     263:         for (var, value) in &cmd.env_vars {
     264:             // Check if this is an associative array assignment like map[foo]=bar
     265:             if let Some((array_name, key)) = generator.extract_array_key(var) {
>>>  266:                 let val = generator.perl_string_literal(value);
     267:                 // For associative array assignments, generate $array{key} = value instead of $ENV{var}
     268:                 output.push_str(&format!("${}{{{}}} = {};\n", array_name, key, val));
     269:             } else if let Word::Literal(s) = value {

File: generator\redirects.rs:278
Pattern: perl_string_literal
Context:
     275:                     output.push_str(&format!("@{} = ({});\n", var, elements_perl.join(", ")));
     276:                 } else {
     277:                     // Regular string assignment
>>>  278:                     let val = generator.perl_string_literal(value);
     279:                     // Declare the variable if it's not already declared
     280:                     if !generator.declared_locals.contains(var) {
     281:                         output.push_str(&format!("my ${} = {};\n", var, val));

File: generator\redirects.rs:290
Pattern: perl_string_literal
Context:
     287:                     output.push_str(&format!("local $ENV{{{}}} = {};;\n", var, val));
     288:                 }
     289:             } else {
>>>  290:                 let val = generator.perl_string_literal(value);
     291:                 // Declare the variable if it's not already declared
     292:                 if !generator.declared_locals.contains(var) {
     293:                     output.push_str(&format!("my ${} = {};\n", var, val));

File: generator\utils.rs:44
Pattern: perl_string_literal
Context:
      41:     None
      42: }
      43: 
>>>   44: pub fn perl_string_literal_impl(generator: &mut Generator, word: &Word) -> String {
      45:     match word {
      46:         Word::Literal(s) => {
      47:             // Escape quotes and backslashes for Perl string literals

File: generator\commands\cat.rs:14
Pattern: perl_string_literal
Context:
      11:             has_heredoc = true;
      12:             if let Some(body) = &redir.heredoc_body {
      13:                 // Print the heredoc content directly
>>>   14:                 output.push_str(&format!("print {};\n", generator.perl_string_literal(&Word::Literal(body.clone()))));
      15:             }
      16:         }
      17:     }

File: generator\commands\pipeline_commands.rs:90
Pattern: perl_string_literal
Context:
      87:                             if cmd_name == "echo" {
      88:                                 // For echo commands, append to output variable
      89:                                 let echo_args: Vec<String> = simple_cmd.args.iter()
>>>   90:                                     .map(|arg| generator.perl_string_literal(arg))
      91:                                     .collect();
      92:                                 let echo_output = echo_args.join(" . ");
      93:                                 output.push_str(&generator.indent());

File: generator\commands\pipeline_commands.rs:377
Pattern: perl_string_literal
Context:
     374:         if let Word::Literal(name) = &first_cmd.name {
     375:             if name == "cat" && !first_cmd.args.is_empty() {
     376:                 // First command is 'cat filename', so read from the file instead of STDIN
>>>  377:                 let filename = generator.perl_string_literal(&first_cmd.args[0]);
     378:                 // For relative filenames, use current directory
     379:                 let adjusted_filename = if !filename.contains('/') && !filename.starts_with('.') && filename != "\"\"" {
     380:                     format!("\"./{}\"", filename.trim_matches('"'))

File: generator\commands\simple_commands.rs:54
Pattern: perl_string_literal
Context:
      51:         for (var, value) in &cmd.env_vars {
      52:             // Check if this is an associative array assignment like map[foo]=bar
      53:             if let Some((array_name, key)) = generator.extract_array_key(var) {
>>>   54:                 let val = generator.perl_string_literal(value);
      55:                 // For associative array assignments, generate $array{key} = value instead of $ENV{var}
      56:                 // Quote the key to avoid bareword errors in strict mode
      57:                 let quoted_key = format!("\"{}\"", generator.escape_perl_string(&key));

File: generator\commands\simple_commands.rs:69
Pattern: perl_string_literal
Context:
      66:                     continue;
      67:                 } else {
      68:                     // Regular string assignment
>>>   69:                     let val = generator.perl_string_literal(value);
      70:                     // Always assign the value, but only declare if not already declared
      71:                     if !generator.declared_locals.contains(var) {
      72:                         output.push_str(&generator.indent());

File: generator\commands\simple_commands.rs:90
Pattern: perl_string_literal
Context:
      87:                 }
      88:             } else {
      89:                 // Handle other Word types
>>>   90:                 let val = generator.perl_string_literal(value);
      91:                 // Always assign the value, but only declare if not already declared
      92:                 if !generator.declared_locals.contains(var) {
      93:                     output.push_str(&generator.indent());

File: generator\commands\simple_commands.rs:140
Pattern: perl_string_literal
Context:
     137:                 let cmd_str = generator.generate_command_string_for_system(&**cmd);
     138:                 output.push_str(&generator.indent());
     139:                 output.push_str(&format!("open(my $pipe, '-|', 'bash', '-c', {});\n",
>>>  140:                     generator.perl_string_literal(&Word::Literal(cmd_str))));
     141:                 output.push_str(&generator.indent());
     142:                 output.push_str(&format!("my $output_ps_{} = <$pipe>;\n", global_counter));
     143:                 output.push_str(&generator.indent());

File: generator\commands\simple_commands.rs:190
Pattern: perl_string_literal
Context:
     187:                     output.push_str(&generator.indent());
     188:                     output.push_str(&format!("open(my ${}, '>', ${}) or die \"Cannot create temp file: $!\\n\";\n", fh_var, temp_var));
     189:                     output.push_str(&generator.indent());
>>>  190:                     output.push_str(&format!("print ${} {};\n", fh_var, generator.perl_string_literal(&Word::Literal(content.clone()))));
     191:                     output.push_str(&generator.indent());
     192:                     output.push_str(&format!("close(${});\n", fh_var));
     193:                 }

File: generator\commands\simple_commands.rs:234
Pattern: perl_string_literal
Context:
     231:                                         // Handle parameter expansion like "${#arr[@]}" -> scalar(@arr)
     232:                                         generator.generate_parameter_expansion(pe)
     233:                                     } else {
>>>  234:                                         generator.perl_string_literal(arg)
     235:                                     }
     236:                                 } else {
     237:                                     generator.perl_string_literal(arg)

File: generator\commands\simple_commands.rs:237
Pattern: perl_string_literal
Context:
     234:                                         generator.perl_string_literal(arg)
     235:                                     }
     236:                                 } else {
>>>  237:                                     generator.perl_string_literal(arg)
     238:                                 }
     239:                             }
     240:                             Word::BraceExpansion(expansion) => {

File: generator\commands\simple_commands.rs:244
Pattern: perl_string_literal
Context:
     241:                                 // Handle brace expansion like {1..5} -> "1 2 3 4 5"
     242:                                 handle_brace_expansion_for_echo(generator, expansion)
     243:                             }
>>>  244:                             _ => generator.perl_string_literal(arg)
     245:                         }
     246:                     })
     247:                     .collect();

File: generator\commands\simple_commands.rs:308
Pattern: perl_string_literal
Context:
     305:                     },
     306:                     _ => {
     307:                         // Handle other value types
>>>  308:                         let val = generator.perl_string_literal(value);
     309:                         if !generator.declared_locals.contains(var) {
     310:                             output.push_str(&generator.indent());
     311:                             output.push_str(&format!("my ${} = {};\n", var, val));

File: generator\commands\simple_commands.rs:454
Pattern: perl_string_literal
Context:
     451:                                 // Handle parameter expansion like "${#arr[@]}" -> scalar(@arr)
     452:                                 generator.generate_parameter_expansion(pe)
     453:                             } else {
>>>  454:                                 generator.perl_string_literal(arg)
     455:                             }
     456:                         } else {
     457:                             generator.perl_string_literal(arg)

File: generator\commands\simple_commands.rs:457
Pattern: perl_string_literal
Context:
     454:                                 generator.perl_string_literal(arg)
     455:                             }
     456:                         } else {
>>>  457:                             generator.perl_string_literal(arg)
     458:                         }
     459:                     }
     460:                     Word::BraceExpansion(expansion) => {

File: generator\commands\simple_commands.rs:484
Pattern: perl_string_literal
Context:
     481:                             // Return as a quoted string literal with proper escaping for Perl
     482:                             format!("\"{}\"", interpreted.replace("\"", "\\\"").replace("\n", "\\n").replace("\t", "\\t").replace("\r", "\\r"))
     483:                         } else {
>>>  484:                             generator.perl_string_literal(arg)
     485:                         }
     486:                     }
     487:                     _ => generator.perl_string_literal(arg)

File: generator\commands\simple_commands.rs:487
Pattern: perl_string_literal
Context:
     484:                             generator.perl_string_literal(arg)
     485:                         }
     486:                     }
>>>  487:                     _ => generator.perl_string_literal(arg)
     488:                 }
     489:             })
     490:             .collect();

File: cli_commands.rs:99
Pattern: convert.*perl
Context:
      96:         input.to_string()
      97:     };
      98: 
>>>   99:     println!("Converting to Perl: {}", content);
     100:     println!("Perl Code:");
     101:     println!("{}", "=".repeat(50));
     102: 

File: cli_commands.rs:118
Pattern: convert.*perl
Context:
     115: }
     116: 
     117: pub fn parse_file_to_perl(filename: &str) {
>>>  118:     println!("Converting file to Perl: {}", filename);
     119: 
     120:     match fs::read_to_string(filename) {
     121:         Ok(content) => {

File: cli_commands.rs:122
Pattern: convert.*perl
Context:
     119: 
     120:     match fs::read_to_string(filename) {
     121:         Ok(content) => {
>>>  122:             println!("Converting to Perl: {}", content);
     123:             println!("Perl Code:");
     124:             println!("{}", "=".repeat(50));
     125: 

File: help.rs:17
Pattern: convert.*perl
Context:
      14:     println!();
      15:     println!("TRANSLATION OPTIONS:");
      16:     println!();
>>>   17:     println!("  parse --perl <input>           - Convert shell script to Perl");
      18: 
      19:     println!();
      20:     println!("  file --perl <filename>         - Convert shell script file to Perl");

File: help.rs:20
Pattern: convert.*perl
Context:
      17:     println!("  parse --perl <input>           - Convert shell script to Perl");
      18: 
      19:     println!();
>>>   20:     println!("  file --perl <filename>         - Convert shell script file to Perl");
      21: 
      22:     println!();
      23:     println!("EXECUTION OPTIONS:");

File: help.rs:74
Pattern: convert.*perl
Context:
      71:     println!("DIRECT EXECUTION EXAMPLES:");
      72:     println!("  {} examples/simple.sh           - Run shell script directly", program_name);
      73:     println!("  {} 'echo Hello World!'          - Run shell command directly", program_name);
>>>   74:     println!("  {} -i myfile.txt -o output.pl   - Convert input file to Perl output file", program_name);
      75:     println!("  {} -i script.txt                - Convert input file and run generated Perl", program_name);
      76:     println!();
      77:     println!("DESCRIPTION:");

File: help.rs:75
Pattern: convert.*perl
Context:
      72:     println!("  {} examples/simple.sh           - Run shell script directly", program_name);
      73:     println!("  {} 'echo Hello World!'          - Run shell command directly", program_name);
      74:     println!("  {} -i myfile.txt -o output.pl   - Convert input file to Perl output file", program_name);
>>>   75:     println!("  {} -i script.txt                - Convert input file and run generated Perl", program_name);
      76:     println!();
      77:     println!("DESCRIPTION:");
      78:     println!("  sh2perl is a tool that translates shell scripts to Perl. It can parse shell syntax,");

File: wasm.rs:63
Pattern: convert.*perl
Context:
      60:         }
      61:     }
      62: 
>>>   63:     /// Convert shell script to Perl
      64:     pub fn to_perl(&mut self, input: &str) -> Result<String, JsValue> {
      65:         let mut parser = Parser::new(input);
      66:         match parser.parse() {

File: generator\control_flow.rs:70
Pattern: convert.*perl
Context:
      67: pub fn generate_case_statement_impl(generator: &mut Generator, case_stmt: &CaseStatement) -> String {
      68:     let mut output = String::new();
      69: 
>>>   70:     // Convert bash case statement to Perl if/elsif/else
      71:     let mut first_case = true;
      72: 
      73:     for case_clause in &case_stmt.cases {

File: generator\control_flow.rs:92
Pattern: convert.*perl
Context:
      89:                 // Default case - this should be the last one
      90:                 pattern_conditions.push("1".to_string()); // Always true
      91:             } else {
>>>   92:                 // Convert bash glob patterns to Perl regex
      93:                 let mut perl_pattern = pattern_str.trim_matches('"').to_string();
      94:                 perl_pattern = perl_pattern.replace("*", ".*");
      95:                 perl_pattern = perl_pattern.replace("?", ".");

File: generator\control_flow.rs:261
Pattern: convert.*perl
Context:
     258:                 if expansion.items.len() == 1 {
     259:                     match &expansion.items[0] {
     260:                         BraceItem::Range(range) => {
>>>  261:                             // Convert {1..5} to Perl range syntax (1..5)
     262:                             if let (Ok(start_num), Ok(end_num)) = (range.start.parse::<i64>(), range.end.parse::<i64>()) {
     263:                                 let step = range.step.as_ref().and_then(|s| s.parse::<i64>().ok()).unwrap_or(1);
     264:                                 if step == 1 {

File: generator\mod.rs:182
Pattern: convert.*perl
Context:
     179:         utils::perl_string_literal_impl(self, word)
     180:     }
     181: 
>>>  182:     pub fn strip_shell_quotes_and_convert_to_perl(&mut self, word: &Word) -> String {
     183:         utils::strip_shell_quotes_and_convert_to_perl_impl(self, word)
     184:     }
     185: 

File: generator\mod.rs:183
Pattern: convert.*perl
Context:
     180:     }
     181: 
     182:     pub fn strip_shell_quotes_and_convert_to_perl(&mut self, word: &Word) -> String {
>>>  183:         utils::strip_shell_quotes_and_convert_to_perl_impl(self, word)
     184:     }
     185: 
     186:     pub fn strip_shell_quotes_for_regex(&mut self, word: &Word) -> String {

File: generator\mod.rs:216
Pattern: convert.*perl
Context:
     213:         words::brace_item_to_word_impl(self, item)
     214:     }
     215: 
>>>  216:     pub fn convert_string_interpolation_to_perl(&self, interp: &StringInterpolation) -> String {
     217:         words::convert_string_interpolation_to_perl_impl(self, interp)
     218:     }
     219: 

File: generator\mod.rs:217
Pattern: convert.*perl
Context:
     214:     }
     215: 
     216:     pub fn convert_string_interpolation_to_perl(&self, interp: &StringInterpolation) -> String {
>>>  217:         words::convert_string_interpolation_to_perl_impl(self, interp)
     218:     }
     219: 
     220:     pub fn convert_arithmetic_to_perl(&self, expr: &str) -> String {

File: generator\mod.rs:220
Pattern: convert.*perl
Context:
     217:         words::convert_string_interpolation_to_perl_impl(self, interp)
     218:     }
     219: 
>>>  220:     pub fn convert_arithmetic_to_perl(&self, expr: &str) -> String {
     221:         words::convert_arithmetic_to_perl_impl(self, expr)
     222:     }
     223: 

File: generator\mod.rs:221
Pattern: convert.*perl
Context:
     218:     }
     219: 
     220:     pub fn convert_arithmetic_to_perl(&self, expr: &str) -> String {
>>>  221:         words::convert_arithmetic_to_perl_impl(self, expr)
     222:     }
     223: 
     224:     pub fn convert_extglob_to_perl_regex(&self, pattern: &str) -> String {

File: generator\mod.rs:224
Pattern: convert.*perl
Context:
     221:         words::convert_arithmetic_to_perl_impl(self, expr)
     222:     }
     223: 
>>>  224:     pub fn convert_extglob_to_perl_regex(&self, pattern: &str) -> String {
     225:         test_expressions::convert_extglob_to_perl_regex_impl(self, pattern)
     226:     }
     227: 

File: generator\mod.rs:225
Pattern: convert.*perl
Context:
     222:     }
     223: 
     224:     pub fn convert_extglob_to_perl_regex(&self, pattern: &str) -> String {
>>>  225:         test_expressions::convert_extglob_to_perl_regex_impl(self, pattern)
     226:     }
     227: 
     228:     pub fn convert_glob_to_regex(&self, pattern: &str) -> String {

File: generator\redirects.rs:307
Pattern: convert.*perl
Context:
     304:     // Generate the builtin command
     305:     match cmd.name.as_str() {
     306:         "set" => {
>>>  307:             // Convert shell set options to Perl equivalents
     308:             for arg in &cmd.args {
     309:                 if let Word::Literal(opt) = arg {
     310:                     match opt.as_str() {

File: generator\test_expressions.rs:4
Pattern: convert.*perl
Context:
       1: use crate::ast::*;
       2: use super::Generator;
       3: 
>>>    4: // Helper function to convert shell variables to Perl equivalents
       5: fn convert_shell_var_to_perl(var: &str) -> String {
       6:     match var {
       7:         "$#" => "scalar(@ARGV)".to_string(),  // $# -> scalar(@ARGV) for argument count

File: generator\test_expressions.rs:5
Pattern: convert.*perl
Context:
       2: use super::Generator;
       3: 
       4: // Helper function to convert shell variables to Perl equivalents
>>>    5: fn convert_shell_var_to_perl(var: &str) -> String {
       6:     match var {
       7:         "$#" => "scalar(@ARGV)".to_string(),  // $# -> scalar(@ARGV) for argument count
       8:         "$@" => "@ARGV".to_string(),          // $@ -> @ARGV for arguments array

File: generator\test_expressions.rs:34
Pattern: convert.*perl
Context:
      31:             let var = parts[0].trim();
      32:             let pattern = parts[1].trim();
      33: 
>>>   34:             // Convert to Perl regex matching
      35:             format!("{} =~ /{}/", var, pattern)
      36:         } else {
      37:             "0".to_string()

File: generator\test_expressions.rs:48
Pattern: convert.*perl
Context:
      45: 
      46:             if modifiers.extglob {
      47:                 // Handle extglob patterns
>>>   48:                 let regex_pattern = generator.convert_extglob_to_perl_regex(pattern);
      49:                 if modifiers.nocasematch {
      50:                     format!("{} =~ /{}/i", var, regex_pattern)
      51:                 } else {

File: generator\test_expressions.rs:84
Pattern: convert.*perl
Context:
      81:                 // Remove quotes from value if it's a shell variable reference
      82:                 let clean_value = if value.starts_with('"') && value.ends_with('"') && value.contains('$') {
      83:                     let unquoted = value[1..value.len()-1].to_string();
>>>   84:                     // Convert shell variables to Perl environment variables
      85:                     if unquoted == "$HOME" {
      86:                         "$ENV{'HOME'}".to_string()
      87:                     } else {

File: generator\test_expressions.rs:99
Pattern: convert.*perl
Context:
      96:                 // Remove quotes from value if it's a shell variable reference
      97:                 let clean_value = if value.starts_with('"') && value.ends_with('"') && value.contains('$') {
      98:                     let unquoted = value[1..value.len()-1].to_string();
>>>   99:                     // Convert shell variables to Perl environment variables
     100:                     if unquoted == "$HOME" {
     101:                         "$ENV{'HOME'}".to_string()
     102:                     } else {

File: generator\test_expressions.rs:323
Pattern: convert.*perl
Context:
     320:         if parts.len() == 2 {
     321:             let left = parts[0].trim();
     322:             let right = parts[1].trim();
>>>  323:             let left_perl = convert_shell_var_to_perl(left);
     324:             let right_perl = convert_shell_var_to_perl(right);
     325:             format!("({} < {})", left_perl, right_perl)
     326:         } else {

File: generator\test_expressions.rs:324
Pattern: convert.*perl
Context:
     321:             let left = parts[0].trim();
     322:             let right = parts[1].trim();
     323:             let left_perl = convert_shell_var_to_perl(left);
>>>  324:             let right_perl = convert_shell_var_to_perl(right);
     325:             format!("({} < {})", left_perl, right_perl)
     326:         } else {
     327:             "0".to_string()

File: generator\test_expressions.rs:335
Pattern: convert.*perl
Context:
     332:         if parts.len() == 2 {
     333:             let left = parts[0].trim();
     334:             let right = parts[1].trim();
>>>  335:             let left_perl = convert_shell_var_to_perl(left);
     336:             let right_perl = convert_shell_var_to_perl(right);
     337:             format!("({} <= {})", left_perl, right_perl)
     338:         } else {

File: generator\test_expressions.rs:336
Pattern: convert.*perl
Context:
     333:             let left = parts[0].trim();
     334:             let right = parts[1].trim();
     335:             let left_perl = convert_shell_var_to_perl(left);
>>>  336:             let right_perl = convert_shell_var_to_perl(right);
     337:             format!("({} <= {})", left_perl, right_perl)
     338:         } else {
     339:             "0".to_string()

File: generator\test_expressions.rs:347
Pattern: convert.*perl
Context:
     344:         if parts.len() == 2 {
     345:             let left = parts[0].trim();
     346:             let right = parts[1].trim();
>>>  347:             let left_perl = convert_shell_var_to_perl(left);
     348:             let right_perl = convert_shell_var_to_perl(right);
     349:             format!("({} > {})", left_perl, right_perl)
     350:         } else {

File: generator\test_expressions.rs:348
Pattern: convert.*perl
Context:
     345:             let left = parts[0].trim();
     346:             let right = parts[1].trim();
     347:             let left_perl = convert_shell_var_to_perl(left);
>>>  348:             let right_perl = convert_shell_var_to_perl(right);
     349:             format!("({} > {})", left_perl, right_perl)
     350:         } else {
     351:             "0".to_string()

File: generator\test_expressions.rs:359
Pattern: convert.*perl
Context:
     356:         if parts.len() == 2 {
     357:             let left = parts[0].trim();
     358:             let right = parts[1].trim();
>>>  359:             let left_perl = convert_shell_var_to_perl(left);
     360:             let right_perl = convert_shell_var_to_perl(right);
     361:             format!("({} >= {})", left_perl, right_perl)
     362:         } else {

File: generator\test_expressions.rs:360
Pattern: convert.*perl
Context:
     357:             let left = parts[0].trim();
     358:             let right = parts[1].trim();
     359:             let left_perl = convert_shell_var_to_perl(left);
>>>  360:             let right_perl = convert_shell_var_to_perl(right);
     361:             format!("({} >= {})", left_perl, right_perl)
     362:         } else {
     363:             "0".to_string()

File: generator\test_expressions.rs:371
Pattern: convert.*perl
Context:
     368:         if parts.len() == 2 {
     369:             let left = parts[0].trim();
     370:             let right = parts[1].trim();
>>>  371:             let left_perl = convert_shell_var_to_perl(left);
     372:             let right_perl = convert_shell_var_to_perl(right);
     373:             format!("({} == {})", left_perl, right_perl)
     374:         } else {

File: generator\test_expressions.rs:372
Pattern: convert.*perl
Context:
     369:             let left = parts[0].trim();
     370:             let right = parts[1].trim();
     371:             let left_perl = convert_shell_var_to_perl(left);
>>>  372:             let right_perl = convert_shell_var_to_perl(right);
     373:             format!("({} == {})", left_perl, right_perl)
     374:         } else {
     375:             "0".to_string()

File: generator\test_expressions.rs:383
Pattern: convert.*perl
Context:
     380:         if parts.len() == 2 {
     381:             let left = parts[0].trim();
     382:             let right = parts[1].trim();
>>>  383:             let left_perl = convert_shell_var_to_perl(left);
     384:             let right_perl = convert_shell_var_to_perl(right);
     385:             format!("({} != {})", left_perl, right_perl)
     386:         } else {

File: generator\test_expressions.rs:384
Pattern: convert.*perl
Context:
     381:             let left = parts[0].trim();
     382:             let right = parts[1].trim();
     383:             let left_perl = convert_shell_var_to_perl(left);
>>>  384:             let right_perl = convert_shell_var_to_perl(right);
     385:             format!("({} != {})", left_perl, right_perl)
     386:         } else {
     387:             "0".to_string()

File: generator\test_expressions.rs:419
Pattern: convert.*perl
Context:
     416: }
     417: 
     418: // Helper methods for test expressions
>>>  419: pub fn convert_extglob_to_perl_regex_impl(generator: &Generator, pattern: &str) -> String {
     420:     // Convert extglob patterns to Perl regex
     421:     let mut result = pattern.to_string();
     422: 

File: generator\test_expressions.rs:420
Pattern: convert.*perl
Context:
     417: 
     418: // Helper methods for test expressions
     419: pub fn convert_extglob_to_perl_regex_impl(generator: &Generator, pattern: &str) -> String {
>>>  420:     // Convert extglob patterns to Perl regex
     421:     let mut result = pattern.to_string();
     422: 
     423:     // Debug output

File: generator\test_expressions.rs:424
Pattern: convert.*perl
Context:
     421:     let mut result = pattern.to_string();
     422: 
     423:     // Debug output
>>>  424:     eprintln!("DEBUG: convert_extglob_to_perl_regex called with pattern: '{}'", pattern);
     425: 
     426:     // Handle @(pattern1|pattern2) - one of the patterns
     427:     result = result.replace("@(", "(?:");

File: generator\utils.rs:64
Pattern: convert.*perl
Context:
      61:             }
      62:         }
      63:         Word::Arithmetic(expr) => {
>>>   64:             // Handle arithmetic expressions by converting them to Perl
      65:             generator.convert_arithmetic_to_perl(&expr.expression)
      66:         }
      67:         Word::ParameterExpansion(pe) => {

File: generator\utils.rs:65
Pattern: convert.*perl
Context:
      62:         }
      63:         Word::Arithmetic(expr) => {
      64:             // Handle arithmetic expressions by converting them to Perl
>>>   65:             generator.convert_arithmetic_to_perl(&expr.expression)
      66:         }
      67:         Word::ParameterExpansion(pe) => {
      68:             // Handle parameter expansion

File: generator\utils.rs:73
Pattern: convert.*perl
Context:
      70:         }
      71:         Word::StringInterpolation(interp) => {
      72:             // Handle string interpolation
>>>   73:             generator.convert_string_interpolation_to_perl(interp)
      74:         }
      75:         Word::CommandSubstitution(cmd) => {
      76:             // Handle command substitution

File: generator\utils.rs:159
Pattern: convert.*perl
Context:
     156:     }
     157: }
     158: 
>>>  159: pub fn strip_shell_quotes_and_convert_to_perl_impl(generator: &mut Generator, word: &Word) -> String {
     160:     match word {
     161:         Word::Literal(s) => {
     162:             // Strip shell quotes if present and convert to Perl string literal

File: generator\utils.rs:162
Pattern: convert.*perl
Context:
     159: pub fn strip_shell_quotes_and_convert_to_perl_impl(generator: &mut Generator, word: &Word) -> String {
     160:     match word {
     161:         Word::Literal(s) => {
>>>  162:             // Strip shell quotes if present and convert to Perl string literal
     163:             let stripped = if (s.starts_with("'") && s.ends_with("'")) || (s.starts_with("\"") && s.ends_with("\"")) {
     164:                 // Remove the outer quotes
     165:                 &s[1..s.len()-1]

File: generator\utils.rs:179
Pattern: convert.*perl
Context:
     176:             format!("\"{}\"", escaped)
     177:         }
     178:         Word::Arithmetic(expr) => {
>>>  179:             // Handle arithmetic expressions by converting them to Perl
     180:             generator.convert_arithmetic_to_perl(&expr.expression)
     181:         }
     182:         Word::ParameterExpansion(pe) => {

File: generator\utils.rs:180
Pattern: convert.*perl
Context:
     177:         }
     178:         Word::Arithmetic(expr) => {
     179:             // Handle arithmetic expressions by converting them to Perl
>>>  180:             generator.convert_arithmetic_to_perl(&expr.expression)
     181:         }
     182:         Word::ParameterExpansion(pe) => {
     183:             // Handle parameter expansion

File: generator\utils.rs:188
Pattern: convert.*perl
Context:
     185:         }
     186:         Word::StringInterpolation(interp) => {
     187:             // Handle string interpolation
>>>  188:             generator.convert_string_interpolation_to_perl(interp)
     189:         }
     190:         _ => format!("{:?}", word)
     191:     }

File: generator\utils.rs:206
Pattern: convert.*perl
Context:
     203:             }
     204:         }
     205:         Word::Arithmetic(expr) => {
>>>  206:             // Handle arithmetic expressions by converting them to Perl
     207:             generator.convert_arithmetic_to_perl(&expr.expression)
     208:         }
     209:         Word::ParameterExpansion(pe) => {

File: generator\utils.rs:207
Pattern: convert.*perl
Context:
     204:         }
     205:         Word::Arithmetic(expr) => {
     206:             // Handle arithmetic expressions by converting them to Perl
>>>  207:             generator.convert_arithmetic_to_perl(&expr.expression)
     208:         }
     209:         Word::ParameterExpansion(pe) => {
     210:             // Handle parameter expansion

File: generator\utils.rs:222
Pattern: convert.*perl
Context:
     219:                     s.clone()
     220:                 } else {
     221:                     // Fall back to normal string interpolation handling
>>>  222:                     generator.convert_string_interpolation_to_perl(interp)
     223:                 }
     224:             } else {
     225:                 // Fall back to normal string interpolation handling

File: generator\utils.rs:226
Pattern: convert.*perl
Context:
     223:                 }
     224:             } else {
     225:                 // Fall back to normal string interpolation handling
>>>  226:                 generator.convert_string_interpolation_to_perl(interp)
     227:             }
     228:         }
     229:         _ => format!("{:?}", word)

File: generator\words.rs:25
Pattern: convert.*perl
Context:
      22:                 .join(", ");
      23:             format!("@{} = ({});", name, elements_str)
      24:         },
>>>   25:         Word::StringInterpolation(interp) => generator.convert_string_interpolation_to_perl(interp),
      26:         Word::Arithmetic(expr) => generator.convert_arithmetic_to_perl(&expr.expression),
      27:         Word::BraceExpansion(expansion) => {
      28:             let expanded = generator.handle_brace_expansion(expansion);

File: generator\words.rs:26
Pattern: convert.*perl
Context:
      23:             format!("@{} = ({});", name, elements_str)
      24:         },
      25:         Word::StringInterpolation(interp) => generator.convert_string_interpolation_to_perl(interp),
>>>   26:         Word::Arithmetic(expr) => generator.convert_arithmetic_to_perl(&expr.expression),
      27:         Word::BraceExpansion(expansion) => {
      28:             let expanded = generator.handle_brace_expansion(expansion);
      29:             // Quote the result since it's used in contexts where quotes are needed

File: generator\words.rs:247
Pattern: convert.*perl
Context:
     244:     }
     245: }
     246: 
>>>  247: pub fn convert_string_interpolation_to_perl_impl(generator: &Generator, interp: &StringInterpolation) -> String {
     248:     // Convert string interpolation to a single Perl interpolated string
     249:     let mut combined_string = String::new();
     250: 

File: generator\words.rs:248
Pattern: convert.*perl
Context:
     245: }
     246: 
     247: pub fn convert_string_interpolation_to_perl_impl(generator: &Generator, interp: &StringInterpolation) -> String {
>>>  248:     // Convert string interpolation to a single Perl interpolated string
     249:     let mut combined_string = String::new();
     250: 
     251:     for part in &interp.parts {

File: generator\words.rs:285
Pattern: convert.*perl
Context:
     282:             }
     283:             StringPart::ParameterExpansion(pe) => {
     284:                 // Handle parameter expansions like ${arr[1]}, ${#arr[@]}, etc.
>>>  285:                 // We need to convert the ParameterExpansion to Perl code
     286:                 // For now, let's handle the common cases directly
     287: 
     288:                 // Check for special array operations first

File: generator\words.rs:355
Pattern: convert.*perl
Context:
     352:     format!("\"{}\"", combined_string)
     353: }
     354: 
>>>  355: pub fn convert_arithmetic_to_perl_impl(_generator: &Generator, expr: &str) -> String {
     356:     // Convert shell arithmetic expression to Perl syntax
     357:     let result = expr.to_string();
     358: 

File: generator\words.rs:356
Pattern: convert.*perl
Context:
     353: }
     354: 
     355: pub fn convert_arithmetic_to_perl_impl(_generator: &Generator, expr: &str) -> String {
>>>  356:     // Convert shell arithmetic expression to Perl syntax
     357:     let result = expr.to_string();
     358: 
     359:     // Convert shell variables to Perl variables (e.g., i -> $i)

File: generator\words.rs:359
Pattern: convert.*perl
Context:
     356:     // Convert shell arithmetic expression to Perl syntax
     357:     let result = expr.to_string();
     358: 
>>>  359:     // Convert shell variables to Perl variables (e.g., i -> $i)
     360:     // Use regex to find variable names and replace them with Perl variable syntax
     361: 
     362:     // Create a regex to match variable names (letters followed by alphanumeric/underscore)

File: generator\commands\grep.rs:210
Pattern: convert.*perl
Context:
     207:                 let mut conditions = Vec::new();
     208: 
     209:                 if let Some(ref include_pat) = include_pattern {
>>>  210:                     // Convert shell glob pattern to Perl regex
     211:                     // *.txt -> .*\.txt$
     212:                     let mut regex_pattern = include_pat.clone();
     213:                     // Replace * with .* first

File: generator\commands\grep.rs:230
Pattern: convert.*perl
Context:
     227:                 }
     228: 
     229:                 if let Some(ref exclude_pat) = exclude_pattern {
>>>  230:                     // Convert shell glob pattern to Perl regex for exclusion
     231:                     let mut regex_pattern = exclude_pat.clone();
     232:                     regex_pattern = regex_pattern.replace("*", ".*");
     233:                     regex_pattern = regex_pattern.replace(".", "\\.");

File: generator\commands\grep.rs:371
Pattern: convert.*perl
Context:
     368:                 escaped_pattern
     369:             };
     370: 
>>>  371:             // Convert shell regex patterns to Perl regex patterns
     372:             // Convert \+ to + (shell extended regex to Perl)
     373:             regex_pattern = regex_pattern.replace("\\+", "+");
     374:             // Convert \? to ? (shell extended regex to Perl)

File: generator\commands\grep.rs:372
Pattern: convert.*perl
Context:
     369:             };
     370: 
     371:             // Convert shell regex patterns to Perl regex patterns
>>>  372:             // Convert \+ to + (shell extended regex to Perl)
     373:             regex_pattern = regex_pattern.replace("\\+", "+");
     374:             // Convert \? to ? (shell extended regex to Perl)
     375:             regex_pattern = regex_pattern.replace("\\?", "?");

File: generator\commands\grep.rs:374
Pattern: convert.*perl
Context:
     371:             // Convert shell regex patterns to Perl regex patterns
     372:             // Convert \+ to + (shell extended regex to Perl)
     373:             regex_pattern = regex_pattern.replace("\\+", "+");
>>>  374:             // Convert \? to ? (shell extended regex to Perl)
     375:             regex_pattern = regex_pattern.replace("\\?", "?");
     376:             // Convert \( and \) to ( and ) (shell extended regex to Perl)
     377:             regex_pattern = regex_pattern.replace("\\(", "(");

File: generator\commands\grep.rs:376
Pattern: convert.*perl
Context:
     373:             regex_pattern = regex_pattern.replace("\\+", "+");
     374:             // Convert \? to ? (shell extended regex to Perl)
     375:             regex_pattern = regex_pattern.replace("\\?", "?");
>>>  376:             // Convert \( and \) to ( and ) (shell extended regex to Perl)
     377:             regex_pattern = regex_pattern.replace("\\(", "(");
     378:             regex_pattern = regex_pattern.replace("\\)", ")");
     379:             // Convert \{ and \} to { and } (shell extended regex to Perl)

File: generator\commands\grep.rs:379
Pattern: convert.*perl
Context:
     376:             // Convert \( and \) to ( and ) (shell extended regex to Perl)
     377:             regex_pattern = regex_pattern.replace("\\(", "(");
     378:             regex_pattern = regex_pattern.replace("\\)", ")");
>>>  379:             // Convert \{ and \} to { and } (shell extended regex to Perl)
     380:             regex_pattern = regex_pattern.replace("\\{", "{");
     381:             regex_pattern = regex_pattern.replace("\\}", "}");
     382:             // Convert \| to | (shell extended regex to Perl)

File: generator\commands\grep.rs:382
Pattern: convert.*perl
Context:
     379:             // Convert \{ and \} to { and } (shell extended regex to Perl)
     380:             regex_pattern = regex_pattern.replace("\\{", "{");
     381:             regex_pattern = regex_pattern.replace("\\}", "}");
>>>  382:             // Convert \| to | (shell extended regex to Perl)
     383:             regex_pattern = regex_pattern.replace("\\|", "|");
     384: 
     385:             // Apply grep filtering

File: generator\commands\simple_commands.rs:209
Pattern: convert.*perl
Context:
     206:                 output.push_str(&generator.indent());
     207:                 output.push_str("print \"\\n\";\n");
     208:             } else {
>>>  209:                 // Convert arguments to Perl format using the dedicated echo function
     210:                 let args: Vec<String> = cmd.args.iter()
     211:                     .map(|arg| {
     212:                         // For echo commands, handle special variables differently

File: generator\commands\simple_commands.rs:274
Pattern: convert.*perl
Context:
     271:             for (var, value) in &cmd.env_vars {
     272:                 match value {
     273:                     Word::Arithmetic(expr) => {
>>>  274:                         // Convert arithmetic expression to Perl
     275:                         let perl_expr = generator.convert_arithmetic_to_perl(&expr.expression);
     276:                         if !generator.declared_locals.contains(var) {
     277:                             // Check if this variable is used in the arithmetic expression

File: generator\commands\simple_commands.rs:275
Pattern: convert.*perl
Context:
     272:                 match value {
     273:                     Word::Arithmetic(expr) => {
     274:                         // Convert arithmetic expression to Perl
>>>  275:                         let perl_expr = generator.convert_arithmetic_to_perl(&expr.expression);
     276:                         if !generator.declared_locals.contains(var) {
     277:                             // Check if this variable is used in the arithmetic expression
     278:                             // If so, we need to initialize it to 0 first

File: generator\commands\simple_commands.rs:429
Pattern: convert.*perl
Context:
     426:             }
     427:         }).collect();
     428: 
>>>  429:         // Convert arguments to Perl format
     430:         let args: Vec<String> = filtered_args.iter()
     431:             .map(|arg| {
     432:                 // For echo commands, handle special variables differently

File: generator\commands\simple_commands.rs:700
Pattern: convert.*perl
Context:
     697:                 expansions.push(expanded);
     698:             }
     699:             _ => {
>>>  700:                 // Convert non-brace arguments to Perl strings
     701:                 non_brace_args.push(generator.word_to_perl(arg));
     702:             }
     703:         }

File: generator\commands\tr.rs:73
Pattern: convert.*perl
Context:
      70: 
      71:     if delete_mode && args.len() >= 1 {
      72:         // tr -d SET1: delete characters in SET1
>>>   73:         let set1 = generator.strip_shell_quotes_and_convert_to_perl(&args[0]);
      74: 
      75:         output.push_str(&format!("my $set1 = {};\n", set1));
      76:         output.push_str(&format!("my $input = ${};\n", input_var));

File: generator\commands\tr.rs:89
Pattern: convert.*perl
Context:
      86:         output.push_str(&format!("$tr_result_{} .= \"\\n\" unless $tr_result_{} =~ /\\n$/ || $tr_result_{} eq '';\n", command_index, command_index, command_index));
      87:     } else if args.len() >= 2 {
      88:         // tr SET1 SET2: translate characters
>>>   89:         let set1 = generator.strip_shell_quotes_and_convert_to_perl(&args[0]);
      90:         let set2 = generator.strip_shell_quotes_and_convert_to_perl(&args[1]);
      91: 
      92:         output.push_str(&format!("my $set1 = {};\n", set1));

File: generator\commands\tr.rs:90
Pattern: convert.*perl
Context:
      87:     } else if args.len() >= 2 {
      88:         // tr SET1 SET2: translate characters
      89:         let set1 = generator.strip_shell_quotes_and_convert_to_perl(&args[0]);
>>>   90:         let set2 = generator.strip_shell_quotes_and_convert_to_perl(&args[1]);
      91: 
      92:         output.push_str(&format!("my $set1 = {};\n", set1));
      93:         output.push_str(&format!("my $set2 = {};\n", set2));

File: cache.rs:26
Pattern: generate.*perl
Context:
      23:     pub stdout: String,
      24:     pub stderr: String,
      25:     pub exit_code: i32,
>>>   26:     pub perl_code_hash: String, // SHA256 hash of the generated Perl code
      27:     pub last_modified: u64, // Unix timestamp
      28: }
      29: 

File: help.rs:75
Pattern: generate.*perl
Context:
      72:     println!("  {} examples/simple.sh           - Run shell script directly", program_name);
      73:     println!("  {} 'echo Hello World!'          - Run shell command directly", program_name);
      74:     println!("  {} -i myfile.txt -o output.pl   - Convert input file to Perl output file", program_name);
>>>   75:     println!("  {} -i script.txt                - Convert input file and run generated Perl", program_name);
      76:     println!();
      77:     println!("DESCRIPTION:");
      78:     println!("  sh2perl is a tool that translates shell scripts to Perl. It can parse shell syntax,");

File: help.rs:79
Pattern: generate.*perl
Context:
      76:     println!();
      77:     println!("DESCRIPTION:");
      78:     println!("  sh2perl is a tool that translates shell scripts to Perl. It can parse shell syntax,");
>>>   79:     println!("  generate equivalent Perl code, and optionally run the generated code to verify");
      80:     println!("  correctness against the original shell script.");
      81:     println!();
      82:         println!("  The tool supports Perl as the target language. It can also generate pseudocode");

File: main.rs:24
Pattern: generate.*perl
Context:
      21: use crate::utils::generate_unified_diff;
      22: use crate::testing::{test_all_examples, test_all_examples_next_fail, find_uses_of_system,
      23:                     test_file_equivalence, AstFormatOptions};
>>>   24: use crate::cli_commands::{run_generated, lex_input, parse_input, parse_file, parse_to_perl,
      25:                      parse_file_to_perl, interactive_mode};
      26: use crate::help::show_help;
      27: 

File: main.rs:128
Pattern: generate.*perl
Context:
     125:                         }
     126:                     };
     127: 
>>>  128:                     // Generate Perl code
     129:                     let mut gen = Generator::new();
     130:                     let code = gen.generate(&commands);
     131: 

File: main.rs:136
Pattern: generate.*perl
Context:
     133:                     if let Some(output_filename) = &output_file {
     134:                         // Write to output file with UTF-8 encoding
     135:                         match shared_utils::SharedUtils::write_utf8_file(output_filename, &code) {
>>>  136:                             Ok(_) => println!("Generated Perl code written to: {} (UTF-8 encoded)", output_filename),
     137:                             Err(e) => println!("Error writing to output file {}: {}", output_filename, e),
     138:                         }
     139:                     } else {

File: main.rs:141
Pattern: generate.*perl
Context:
     138:                         }
     139:                     } else {
     140:                         // Show generated code and run it
>>>  141:                         println!("Generated Perl code:");
     142:                         println!("{}", code);
     143:                         println!("\n--- Running generated Perl code ---");
     144:                         let tmp = "__tmp_run.pl";

File: main.rs:143
Pattern: generate.*perl
Context:
     140:                         // Show generated code and run it
     141:                         println!("Generated Perl code:");
     142:                         println!("{}", code);
>>>  143:                         println!("\n--- Running generated Perl code ---");
     144:                         let tmp = "__tmp_run.pl";
     145:                         if shared_utils::SharedUtils::write_utf8_file(tmp, &code).is_ok() {
     146:                             let _ = std::process::Command::new("perl").arg(tmp).status();

File: main.rs:438
Pattern: generate.*perl
Context:
     435:                             }
     436:                         };
     437: 
>>>  438:                         // Generate Perl code
     439:                         let mut gen = Generator::new();
     440:                         let code = gen.generate(&commands);
     441: 

File: main.rs:446
Pattern: generate.*perl
Context:
     443:                         if let Some(output_filename) = &output_file {
     444:                             // Write to output file with UTF-8 encoding
     445:                             match shared_utils::SharedUtils::write_utf8_file(output_filename, &code) {
>>>  446:                                 Ok(_) => println!("Generated Perl code written to: {} (UTF-8 encoded)", output_filename),
     447:                                 Err(e) => println!("Error writing to output file {}: {}", output_filename, e),
     448:                             }
     449:                         } else {

File: main.rs:451
Pattern: generate.*perl
Context:
     448:                             }
     449:                         } else {
     450:                             // Show generated code and run it
>>>  451:                             println!("Generated Perl code:");
     452:                             println!("{}", code);
     453:                             println!("\n--- Running generated Perl code ---");
     454:                             let tmp = "__tmp_run.pl";

File: main.rs:453
Pattern: generate.*perl
Context:
     450:                             // Show generated code and run it
     451:                             println!("Generated Perl code:");
     452:                             println!("{}", code);
>>>  453:                             println!("\n--- Running generated Perl code ---");
     454:                             let tmp = "__tmp_run.pl";
     455:                             if shared_utils::SharedUtils::write_utf8_file(tmp, &code).is_ok() {
     456:                                 let _ = std::process::Command::new("perl").arg(tmp).status();

File: main.rs:479
Pattern: generate.*perl
Context:
     476:                             }
     477:                         };
     478: 
>>>  479:                         // Generate Perl code
     480:                         let mut gen = Generator::new();
     481:                         let code = gen.generate(&commands);
     482: 

File: main.rs:487
Pattern: generate.*perl
Context:
     484:                         if let Some(output_filename) = &output_file {
     485:                             // Write to output file with UTF-8 encoding
     486:                             match shared_utils::SharedUtils::write_utf8_file(output_filename, &code) {
>>>  487:                                 Ok(_) => println!("Generated Perl code written to: {} (UTF-8 encoded)", output_filename),
     488:                                 Err(e) => println!("Error writing to output file {}: {}", output_filename, e),
     489:                             }
     490:                         } else {

File: main.rs:492
Pattern: generate.*perl
Context:
     489:                             }
     490:                         } else {
     491:                             // Show generated code and run it
>>>  492:                             println!("Generated Perl code:");
     493:                             println!("{}", code);
     494:                             println!("\n--- Running generated Perl code ---");
     495:                             let tmp = "__tmp_run.pl";

File: main.rs:494
Pattern: generate.*perl
Context:
     491:                             // Show generated code and run it
     492:                             println!("Generated Perl code:");
     493:                             println!("{}", code);
>>>  494:                             println!("\n--- Running generated Perl code ---");
     495:                             let tmp = "__tmp_run.pl";
     496:                             if shared_utils::SharedUtils::write_utf8_file(tmp, &code).is_ok() {
     497:                                 let _ = std::process::Command::new("perl").arg(tmp).status();

File: main.rs:514
Pattern: generate.*perl
Context:
     511:                 // Parse the command as shell input
     512:                 match Parser::new(command).parse() {
     513:                     Ok(commands) => {
>>>  514:                         // Generate Perl code
     515:                         let mut generator = Generator::new();
     516:                         let perl_code = generator.generate(&commands);
     517: 

File: main.rs:521
Pattern: generate.*perl
Context:
     518:                         // Write to temporary file and execute
     519:                         let tmp_file = "__tmp_direct_exec.pl";
     520:                         if shared_utils::SharedUtils::write_utf8_file(tmp_file, &perl_code).is_ok() {
>>>  521:                             println!("Generated Perl code:");
     522:                             println!("{}", perl_code);
     523:                             println!("\n--- Running generated Perl code ---");
     524: 

File: main.rs:523
Pattern: generate.*perl
Context:
     520:                         if shared_utils::SharedUtils::write_utf8_file(tmp_file, &perl_code).is_ok() {
     521:                             println!("Generated Perl code:");
     522:                             println!("{}", perl_code);
>>>  523:                             println!("\n--- Running generated Perl code ---");
     524: 
     525:                             match std::process::Command::new("perl").arg(tmp_file).output() {
     526:                                 Ok(output) => {

File: utils.rs:147
Pattern: generate.*perl
Context:
     144:     diff
     145: }
     146: 
>>>  147: /// Check if the generated Perl code contains forbidden patterns specified in PERL_MUST_NOT_CONTAIN comments
     148: pub fn check_perl_must_not_contain(shell_content: &str, perl_code: &str) -> Result<(), String> {
     149:     let lines: Vec<&str> = shell_content.lines().collect();
     150:     let mut violations = Vec::new();

File: utils.rs:158
Pattern: generate.*perl
Context:
     155:             if let Some(pattern_start) = line.find("#PERL_MUST_NOT_CONTAIN:") {
     156:                 let pattern = line[pattern_start + "#PERL_MUST_NOT_CONTAIN:".len()..].trim();
     157:                 if !pattern.is_empty() {
>>>  158:                     // Check if the forbidden pattern exists in the generated Perl code
     159:                     if perl_code.contains(pattern) {
     160:                         violations.push(format!("Line {}: Found forbidden pattern '{}' in generated Perl code", line_num + 1, pattern));
     161:                     }

File: utils.rs:160
Pattern: generate.*perl
Context:
     157:                 if !pattern.is_empty() {
     158:                     // Check if the forbidden pattern exists in the generated Perl code
     159:                     if perl_code.contains(pattern) {
>>>  160:                         violations.push(format!("Line {}: Found forbidden pattern '{}' in generated Perl code", line_num + 1, pattern));
     161:                     }
     162:                 }
     163:             }

File: bin\test_linebyline.rs:56
Pattern: generate.*perl
Context:
      53:     println!("\nPipeline 1: cat | tr 'a' 'b' | grep 'hello'");
      54:     if pipeline_supports_linebyline(&pipeline1) {
      55:         println!("  ✓ This pipeline can use line-by-line processing!");
>>>   56:         println!("  → Will generate streaming Perl code");
      57:     } else {
      58:         println!("  ✗ This pipeline cannot use line-by-line processing");
      59:         println!("  → Will fall back to buffered processing");

File: bin\test_linebyline.rs:90
Pattern: generate.*perl
Context:
      87:     println!("\nPipeline 2: cat | sort | grep 'hello'");
      88:     if pipeline_supports_linebyline(&pipeline2) {
      89:         println!("  ✓ This pipeline can use line-by-line processing!");
>>>   90:         println!("  → Will generate streaming Perl code");
      91:     } else {
      92:         println!("  ✗ This pipeline cannot use line-by-line processing");
      93:         println!("  → Will fall back to buffered processing");

File: bin\test_linebyline.rs:119
Pattern: generate.*perl
Context:
     116:     println!("\nPipeline 3: cat | custom_script");
     117:     if pipeline_supports_linebyline(&pipeline3) {
     118:         println!("  ✓ This pipeline can use line-by-line processing!");
>>>  119:         println!("  → Will generate streaming Perl code");
     120:     } else {
     121:         println!("  ✗ This pipeline cannot use line-by-line processing");
     122:         println!("  → Will fall back to buffered processing");

File: bin\test_linebyline.rs:136
Pattern: generate.*perl
Context:
     133:     println!("\nWhen line-by-line is not possible:");
     134:     println!("  • Falls back to buffered processing");
     135:     println!("  • Maintains compatibility");
>>>  136:     println!("  • Still generates optimized Perl code");
     137: }

File: generator\commands\builtins.rs:161
Pattern: generate.*perl
Context:
     158:     true
     159: }
     160: 
>>>  161: /// Generate generic Perl code for a builtin command that doesn't need special handling
     162: /// This is a fallback for commands that don't have specialized modules
     163: pub fn generate_generic_builtin(generator: &mut Generator, cmd: &SimpleCommand, input_var: &str, output_var: &str, command_index: &str, linebyline: bool) -> String {
     164:     let command_name = match &cmd.name {

File: generator\commands\command_dispatcher.rs:170
Pattern: generate.*perl
Context:
     167:                         let is_complex_command = matches!(**cmd, Command::Pipeline(_) | Command::Redirect(_));
     168: 
     169:                         if in_stdout_context || is_complex_command {
>>>  170:                             // If we're already in a STDOUT context, or if this is a complex command, generate the actual Perl code
     171:                             result.push_str(&generator.indent());
     172:                             result.push_str(&format!("my $output_ps_{};\n", global_counter));
     173:                             result.push_str(&generator.indent());

File: generator\commands\logic_commands.rs:139
Pattern: generate.*perl
Context:
     136:         output.push_str("}\n");
     137:         return output;
     138:     } else {
>>>  139:         // For commands that generate Perl code (like grep), we need to handle them specially
     140:         // to avoid embedding Perl code inside shell backticks
     141:         if let Command::Simple(simple_cmd) = left {
     142:             if let Word::Literal(name) = &simple_cmd.name {

File: generator\commands\pipeline_commands.rs:5
Pattern: generate.*perl
Context:
       2: use crate::generator::Generator;
       3: use crate::generator::commands::builtins::{is_builtin, generate_generic_builtin, pipeline_supports_linebyline};
       4: 
>>>    5: /// Helper function to generate Perl code for a command using the builtins registry
       6: fn generate_command_using_builtins(
       7:     generator: &mut Generator,
       8:     command: &Command,

File: generator\commands\printf.rs:53
Pattern: generate.*perl
Context:
      50:                 }
      51:             }
      52: 
>>>   53:             // Generate Perl code to print array elements with the format
      54:             output.push_str(&format!("foreach my $item (@{}) {{\n", array_var));
      55:             output.push_str(&format!("    printf(\"{}\", $item);\n", format_string));
      56:             output.push_str("}\n");

File: generator\commands\simple_commands.rs:404
Pattern: generate.*perl
Context:
     401:     output
     402: }
     403: 
>>>  404: /// Generate Perl code for echo command
     405: pub fn generate_echo_command(generator: &mut Generator, cmd: &SimpleCommand, _input_var: &str, output_var: &str) -> String {
     406:     let mut output = String::new();
     407: 

File: generator\commands\simple_commands.rs:732
Pattern: generate.*perl
Context:
     729:         combinations = new_combinations;
     730:     }
     731: 
>>>  732:     // Generate Perl code to print all combinations
     733:     output.push_str(&generator.indent());
     734:     output.push_str("my @combinations = (\n");
     735: 

File: generator\commands\subshell_commands.rs:7
Pattern: generate.*perl
Context:
       4: pub fn generate_subshell_impl(generator: &mut Generator, command: &Command) -> String {
       5:     let mut output = String::new();
       6: 
>>>    7:     // Generate subshell command using Perl's do block
       8:     output.push_str("do {\n");
       9:     generator.indent_level += 1;
      10:     output.push_str(&generator.indent());

File: generator\commands\subshell_commands.rs:21
Pattern: generate.*perl
Context:
      18: pub fn generate_background_impl(generator: &mut Generator, command: &Command) -> String {
      19:     let mut output = String::new();
      20: 
>>>   21:     // Generate background command using Perl's do block
      22:     output.push_str("do {\n");
      23:     generator.indent_level += 1;
      24:     output.push_str(&generator.indent());

File: generator\commands\system_commands.rs:67
Pattern: generate.*perl
Context:
      64:                     generator.generate_for_loop(for_loop)
      65:                 }
      66:                 _ => {
>>>   67:                     // For complex commands, generate proper Perl code instead of debug representation
      68:                     generator.generate_command(cmd)
      69:                 }
      70:             }

File: generator\commands\system_commands.rs:83
Pattern: generate.*perl
Context:
      80:             "echo 'RedirectCommand cannot be converted to shell command'".to_string()
      81:         }
      82:         _ => {
>>>   83:             // For other complex commands, generate proper Perl code instead of debug representation
      84:             generator.generate_command(cmd)
      85:         }
      86:     }

File: generator\commands\wc.rs:32
Pattern: generate.*perl
Context:
      29:         count_bytes = true;
      30:     }
      31: 
>>>   32:     // Generate Perl code for wc
      33:     output.push_str(&format!("my @wc_lines_{} = split(/\\n/, ${});\n", command_index, input_var));
      34: 
      35:     if count_lines {

File: generator\mod.rs:182
Pattern: strip_shell_quotes_and_convert_to_perl
Context:
     179:         utils::perl_string_literal_impl(self, word)
     180:     }
     181: 
>>>  182:     pub fn strip_shell_quotes_and_convert_to_perl(&mut self, word: &Word) -> String {
     183:         utils::strip_shell_quotes_and_convert_to_perl_impl(self, word)
     184:     }
     185: 

File: generator\mod.rs:183
Pattern: strip_shell_quotes_and_convert_to_perl
Context:
     180:     }
     181: 
     182:     pub fn strip_shell_quotes_and_convert_to_perl(&mut self, word: &Word) -> String {
>>>  183:         utils::strip_shell_quotes_and_convert_to_perl_impl(self, word)
     184:     }
     185: 
     186:     pub fn strip_shell_quotes_for_regex(&mut self, word: &Word) -> String {

File: generator\utils.rs:159
Pattern: strip_shell_quotes_and_convert_to_perl
Context:
     156:     }
     157: }
     158: 
>>>  159: pub fn strip_shell_quotes_and_convert_to_perl_impl(generator: &mut Generator, word: &Word) -> String {
     160:     match word {
     161:         Word::Literal(s) => {
     162:             // Strip shell quotes if present and convert to Perl string literal

File: generator\commands\tr.rs:73
Pattern: strip_shell_quotes_and_convert_to_perl
Context:
      70: 
      71:     if delete_mode && args.len() >= 1 {
      72:         // tr -d SET1: delete characters in SET1
>>>   73:         let set1 = generator.strip_shell_quotes_and_convert_to_perl(&args[0]);
      74: 
      75:         output.push_str(&format!("my $set1 = {};\n", set1));
      76:         output.push_str(&format!("my $input = ${};\n", input_var));

File: generator\commands\tr.rs:89
Pattern: strip_shell_quotes_and_convert_to_perl
Context:
      86:         output.push_str(&format!("$tr_result_{} .= \"\\n\" unless $tr_result_{} =~ /\\n$/ || $tr_result_{} eq '';\n", command_index, command_index, command_index));
      87:     } else if args.len() >= 2 {
      88:         // tr SET1 SET2: translate characters
>>>   89:         let set1 = generator.strip_shell_quotes_and_convert_to_perl(&args[0]);
      90:         let set2 = generator.strip_shell_quotes_and_convert_to_perl(&args[1]);
      91: 
      92:         output.push_str(&format!("my $set1 = {};\n", set1));

File: generator\commands\tr.rs:90
Pattern: strip_shell_quotes_and_convert_to_perl
Context:
      87:     } else if args.len() >= 2 {
      88:         // tr SET1 SET2: translate characters
      89:         let set1 = generator.strip_shell_quotes_and_convert_to_perl(&args[0]);
>>>   90:         let set2 = generator.strip_shell_quotes_and_convert_to_perl(&args[1]);
      91: 
      92:         output.push_str(&format!("my $set1 = {};\n", set1));
      93:         output.push_str(&format!("my $set2 = {};\n", set2));

File: generator\mod.rs:186
Pattern: strip_shell_quotes_for_regex
Context:
     183:         utils::strip_shell_quotes_and_convert_to_perl_impl(self, word)
     184:     }
     185: 
>>>  186:     pub fn strip_shell_quotes_for_regex(&mut self, word: &Word) -> String {
     187:         utils::strip_shell_quotes_for_regex_impl(self, word)
     188:     }
     189: 

File: generator\mod.rs:187
Pattern: strip_shell_quotes_for_regex
Context:
     184:     }
     185: 
     186:     pub fn strip_shell_quotes_for_regex(&mut self, word: &Word) -> String {
>>>  187:         utils::strip_shell_quotes_for_regex_impl(self, word)
     188:     }
     189: 
     190:     pub fn get_unique_file_handle(&mut self) -> String {

File: generator\utils.rs:194
Pattern: strip_shell_quotes_for_regex
Context:
     191:     }
     192: }
     193: 
>>>  194: pub fn strip_shell_quotes_for_regex_impl(generator: &mut Generator, word: &Word) -> String {
     195:     match word {
     196:         Word::Literal(s) => {
     197:             // Strip shell quotes if present and return the raw string for regex

File: generator\commands\pipeline_commands.rs:574
Pattern: strip_shell_quotes_for_regex
Context:
     571:             if let Some(pattern_arg) = cmd.args.iter().find(|arg| {
     572:                 if let Word::Literal(s) = arg { !s.starts_with('-') } else { true }
     573:             }) {
>>>  574:                 let pattern = generator.strip_shell_quotes_for_regex(pattern_arg);
     575:                 output.push_str(&format!("next unless $line =~ /{}/;\n", pattern));
     576:             }
     577:             output

File: generator\commands\tr.rs:34
Pattern: strip_shell_quotes_for_regex
Context:
      31: 
      32:     if delete_mode && args.len() >= 1 {
      33:         // tr -d SET1: delete characters in SET1
>>>   34:         let set1 = generator.strip_shell_quotes_for_regex(&args[0]);
      35: 
      36:         // For line-by-line, process the line directly
      37:         output.push_str(&format!("${} =~ tr/{}/ /d;\n", input_var, set1));

File: generator\commands\tr.rs:40
Pattern: strip_shell_quotes_for_regex
Context:
      37:         output.push_str(&format!("${} =~ tr/{}/ /d;\n", input_var, set1));
      38:     } else if args.len() >= 2 {
      39:         // tr SET1 SET2: translate characters
>>>   40:         let set1 = generator.strip_shell_quotes_for_regex(&args[0]);
      41:         let set2 = generator.strip_shell_quotes_for_regex(&args[1]);
      42: 
      43:         // For line-by-line, process the line directly

File: generator\commands\tr.rs:41
Pattern: strip_shell_quotes_for_regex
Context:
      38:     } else if args.len() >= 2 {
      39:         // tr SET1 SET2: translate characters
      40:         let set1 = generator.strip_shell_quotes_for_regex(&args[0]);
>>>   41:         let set2 = generator.strip_shell_quotes_for_regex(&args[1]);
      42: 
      43:         // For line-by-line, process the line directly
      44:         output.push_str(&format!("${} =~ tr/{}/{}/;\n", input_var, set1, set2));

File: generator\mod.rs:216
Pattern: convert_string_interpolation_to_perl
Context:
     213:         words::brace_item_to_word_impl(self, item)
     214:     }
     215: 
>>>  216:     pub fn convert_string_interpolation_to_perl(&self, interp: &StringInterpolation) -> String {
     217:         words::convert_string_interpolation_to_perl_impl(self, interp)
     218:     }
     219: 

File: generator\mod.rs:217
Pattern: convert_string_interpolation_to_perl
Context:
     214:     }
     215: 
     216:     pub fn convert_string_interpolation_to_perl(&self, interp: &StringInterpolation) -> String {
>>>  217:         words::convert_string_interpolation_to_perl_impl(self, interp)
     218:     }
     219: 
     220:     pub fn convert_arithmetic_to_perl(&self, expr: &str) -> String {

File: generator\utils.rs:73
Pattern: convert_string_interpolation_to_perl
Context:
      70:         }
      71:         Word::StringInterpolation(interp) => {
      72:             // Handle string interpolation
>>>   73:             generator.convert_string_interpolation_to_perl(interp)
      74:         }
      75:         Word::CommandSubstitution(cmd) => {
      76:             // Handle command substitution

File: generator\utils.rs:188
Pattern: convert_string_interpolation_to_perl
Context:
     185:         }
     186:         Word::StringInterpolation(interp) => {
     187:             // Handle string interpolation
>>>  188:             generator.convert_string_interpolation_to_perl(interp)
     189:         }
     190:         _ => format!("{:?}", word)
     191:     }

File: generator\utils.rs:222
Pattern: convert_string_interpolation_to_perl
Context:
     219:                     s.clone()
     220:                 } else {
     221:                     // Fall back to normal string interpolation handling
>>>  222:                     generator.convert_string_interpolation_to_perl(interp)
     223:                 }
     224:             } else {
     225:                 // Fall back to normal string interpolation handling

File: generator\utils.rs:226
Pattern: convert_string_interpolation_to_perl
Context:
     223:                 }
     224:             } else {
     225:                 // Fall back to normal string interpolation handling
>>>  226:                 generator.convert_string_interpolation_to_perl(interp)
     227:             }
     228:         }
     229:         _ => format!("{:?}", word)

File: generator\words.rs:25
Pattern: convert_string_interpolation_to_perl
Context:
      22:                 .join(", ");
      23:             format!("@{} = ({});", name, elements_str)
      24:         },
>>>   25:         Word::StringInterpolation(interp) => generator.convert_string_interpolation_to_perl(interp),
      26:         Word::Arithmetic(expr) => generator.convert_arithmetic_to_perl(&expr.expression),
      27:         Word::BraceExpansion(expansion) => {
      28:             let expanded = generator.handle_brace_expansion(expansion);

File: generator\words.rs:247
Pattern: convert_string_interpolation_to_perl
Context:
     244:     }
     245: }
     246: 
>>>  247: pub fn convert_string_interpolation_to_perl_impl(generator: &Generator, interp: &StringInterpolation) -> String {
     248:     // Convert string interpolation to a single Perl interpolated string
     249:     let mut combined_string = String::new();
     250: 

File: generator\mod.rs:220
Pattern: convert_arithmetic_to_perl
Context:
     217:         words::convert_string_interpolation_to_perl_impl(self, interp)
     218:     }
     219: 
>>>  220:     pub fn convert_arithmetic_to_perl(&self, expr: &str) -> String {
     221:         words::convert_arithmetic_to_perl_impl(self, expr)
     222:     }
     223: 

File: generator\mod.rs:221
Pattern: convert_arithmetic_to_perl
Context:
     218:     }
     219: 
     220:     pub fn convert_arithmetic_to_perl(&self, expr: &str) -> String {
>>>  221:         words::convert_arithmetic_to_perl_impl(self, expr)
     222:     }
     223: 
     224:     pub fn convert_extglob_to_perl_regex(&self, pattern: &str) -> String {

File: generator\utils.rs:65
Pattern: convert_arithmetic_to_perl
Context:
      62:         }
      63:         Word::Arithmetic(expr) => {
      64:             // Handle arithmetic expressions by converting them to Perl
>>>   65:             generator.convert_arithmetic_to_perl(&expr.expression)
      66:         }
      67:         Word::ParameterExpansion(pe) => {
      68:             // Handle parameter expansion

File: generator\utils.rs:180
Pattern: convert_arithmetic_to_perl
Context:
     177:         }
     178:         Word::Arithmetic(expr) => {
     179:             // Handle arithmetic expressions by converting them to Perl
>>>  180:             generator.convert_arithmetic_to_perl(&expr.expression)
     181:         }
     182:         Word::ParameterExpansion(pe) => {
     183:             // Handle parameter expansion

File: generator\utils.rs:207
Pattern: convert_arithmetic_to_perl
Context:
     204:         }
     205:         Word::Arithmetic(expr) => {
     206:             // Handle arithmetic expressions by converting them to Perl
>>>  207:             generator.convert_arithmetic_to_perl(&expr.expression)
     208:         }
     209:         Word::ParameterExpansion(pe) => {
     210:             // Handle parameter expansion

File: generator\words.rs:26
Pattern: convert_arithmetic_to_perl
Context:
      23:             format!("@{} = ({});", name, elements_str)
      24:         },
      25:         Word::StringInterpolation(interp) => generator.convert_string_interpolation_to_perl(interp),
>>>   26:         Word::Arithmetic(expr) => generator.convert_arithmetic_to_perl(&expr.expression),
      27:         Word::BraceExpansion(expansion) => {
      28:             let expanded = generator.handle_brace_expansion(expansion);
      29:             // Quote the result since it's used in contexts where quotes are needed

File: generator\words.rs:355
Pattern: convert_arithmetic_to_perl
Context:
     352:     format!("\"{}\"", combined_string)
     353: }
     354: 
>>>  355: pub fn convert_arithmetic_to_perl_impl(_generator: &Generator, expr: &str) -> String {
     356:     // Convert shell arithmetic expression to Perl syntax
     357:     let result = expr.to_string();
     358: 

File: generator\commands\simple_commands.rs:275
Pattern: convert_arithmetic_to_perl
Context:
     272:                 match value {
     273:                     Word::Arithmetic(expr) => {
     274:                         // Convert arithmetic expression to Perl
>>>  275:                         let perl_expr = generator.convert_arithmetic_to_perl(&expr.expression);
     276:                         if !generator.declared_locals.contains(var) {
     277:                             // Check if this variable is used in the arithmetic expression
     278:                             // If so, we need to initialize it to 0 first

File: generator\mod.rs:224
Pattern: convert_extglob_to_perl_regex
Context:
     221:         words::convert_arithmetic_to_perl_impl(self, expr)
     222:     }
     223: 
>>>  224:     pub fn convert_extglob_to_perl_regex(&self, pattern: &str) -> String {
     225:         test_expressions::convert_extglob_to_perl_regex_impl(self, pattern)
     226:     }
     227: 

File: generator\mod.rs:225
Pattern: convert_extglob_to_perl_regex
Context:
     222:     }
     223: 
     224:     pub fn convert_extglob_to_perl_regex(&self, pattern: &str) -> String {
>>>  225:         test_expressions::convert_extglob_to_perl_regex_impl(self, pattern)
     226:     }
     227: 
     228:     pub fn convert_glob_to_regex(&self, pattern: &str) -> String {

File: generator\test_expressions.rs:48
Pattern: convert_extglob_to_perl_regex
Context:
      45: 
      46:             if modifiers.extglob {
      47:                 // Handle extglob patterns
>>>   48:                 let regex_pattern = generator.convert_extglob_to_perl_regex(pattern);
      49:                 if modifiers.nocasematch {
      50:                     format!("{} =~ /{}/i", var, regex_pattern)
      51:                 } else {

File: generator\test_expressions.rs:419
Pattern: convert_extglob_to_perl_regex
Context:
     416: }
     417: 
     418: // Helper methods for test expressions
>>>  419: pub fn convert_extglob_to_perl_regex_impl(generator: &Generator, pattern: &str) -> String {
     420:     // Convert extglob patterns to Perl regex
     421:     let mut result = pattern.to_string();
     422: 

File: generator\test_expressions.rs:424
Pattern: convert_extglob_to_perl_regex
Context:
     421:     let mut result = pattern.to_string();
     422: 
     423:     // Debug output
>>>  424:     eprintln!("DEBUG: convert_extglob_to_perl_regex called with pattern: '{}'", pattern);
     425: 
     426:     // Handle @(pattern1|pattern2) - one of the patterns
     427:     result = result.replace("@(", "(?:");


