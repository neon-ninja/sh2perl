=== Example 023: Basic mv command ===
Using system() to call mv (move file):
File moved successfully
Content: This is a test file for moving
It has multiple lines
To demonstrate mv functionality

mv with verbose (-v):
renamed 'test_mv_dest.txt' -> 'test_mv_verbose.txt'

mv with force (-f):
File moved with force

mv with interactive (-i):

mv with backup (-b):
File moved with backup

mv with suffix (--suffix=.bak):

mv with no target directory (-T):
File moved with no target directory

mv with update (-u):

mv with no clobber (-n):
File moved with no clobber

mv with strip trailing slashes (--strip-trailing-slashes):

mv with multiple files:

mv with preserve all (-a):
=== Example 023 completed successfully ===
